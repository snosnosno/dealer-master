> ⚠️ **이 문서는 2026-08-24 기준이라 현재 방향과 어긋난다.**
> **정본은 [`PRD.md`](PRD.md) 다.** 수익화 시점·서술형 AI 채점·로드맵이 모두 바뀌었다.
> 이 문서에서 아직 살아 있는 부분은 `PRD.md` §8 에 표로 정리돼 있다.

---

# 딜러마스터 — 전체 유저플로우 검토

**작성일:** 2026-08-20
**참고 다이어그램**: 가입 전 유입 플로우(signup_flow), 가입 후 사용 루프(retention_loop) — 위 대화에서 렌더링됨

---

## 1. 유저 유형별 플로우

### 1-1. 신규 방문자 (비로그인)
```
랜딩페이지 → 무료 미리보기 케이스 1~2개 플레이 → 3번째 시도 시 가입 페이지로 전환
```
- **이탈 포인트**: 무료 케이스 자체가 매력적이지 않으면 가입 전환 없이 이탈 → 무료 케이스 선정이 랜딩페이지 카피보다 중요할 수 있음
- **미결정 사항**: 무료 케이스를 고정할지 매주 교체할지 (마스터 문서 §8 열린 질문에 이미 있음)

### 1-2. 신규 가입자 (결제 전)
```
회원가입 → 구독 결제(토스페이먼츠) → 결제 성공 → 온보딩
```
- **엣지케이스**: 회원가입만 하고 결제를 미루는 유저 — DB상 `subscriptions.status = 'pending'` 상태로 남을 수 있음. 이 상태로 얼마나 유지할지(예: 24시간 후 리마인드 알림) 설계 필요
- **엣지케이스**: 결제 실패(카드 오류 등) — 즉시 재시도 유도 화면 필요, 가입 자체는 유지

### 1-3. 온보딩 (첫 로그인 직후)
```
온보딩 화면 → 등급 시스템 소개(주니어→시니어→마스터) → 축1/2/3 소개 → 대시보드 진입
```
- **미설계 부분**: 온보딩에서 "관심 축"을 먼저 물어볼지(예: 지망생/현직/운영자 선택) — 물어보면 대시보드 초기 추천 콘텐츠를 개인화할 수 있음. PRD에는 없던 내용이라 추가 검토 필요

### 1-4. 재방문 구독자 (핵심 루프)
```
대시보드 → 케이스 플레이(서술형 입력 → AI 채점 → 피드백) → 결과(스트릭/등급/랭킹 갱신) → 대시보드로 복귀
```
- 이 루프가 주 1회 이상 반복되는 게 PRD의 핵심 리텐션 목표 (7일 내 플레이율 60%, 주간 재방문율 40%)
- **엣지케이스**: AI 채점 API 지연/실패 — PRD에 이미 명시된 대로 재시도 안내, 빈 화면 방치 금지

### 1-5. 결제 만료 유저
```
결제 갱신 실패 → 유예 3일(알림 발송) → 접근 차단 → 재구독 유도
```
- **접근 차단 시 정책**: 과거 attempts 기록은 보존, 신규 케이스만 차단 (마스터 문서 §3에 이미 명시)
- **미결정 사항**: 유예 기간 중 알림을 몇 회, 어떤 채널로 보낼지(이메일만? 앱 푸시도?) — 초기엔 이메일만으로 단순화 권장

### 1-6. 해지 유저
```
설정 → 구독 해지 → 해지 확인 → 다음 결제일까지는 접근 유지 → 만료 후 차단
```
- **원칙**: 즉시 차단이 아니라 "이미 결제한 기간까지는 이용 가능"이 표준적인 구독 UX — 이 원칙을 명시적으로 정해두는 게 좋음

---

## 2. 관리자(Sno) 플로우 — 별도 설계 필요

지금까지의 유저플로우 문서들이 전부 "구독자 관점"이었는데, **콘텐츠 발행자 관점의 플로우가 아직 설계되지 않았습니다.** 이게 실제로는 Sno님이 매주 가장 자주 쓰게 될 화면이라 빠지면 안 됩니다.

```
케이스 작성(직접 또는 AI 변형 생성) → status: draft 저장
→ 검수(TDA 조항 재확인) → status: published
→ 구독자에게 노출
```

- **필요 화면**: 관리자 전용 케이스 작성/편집 페이지, draft 목록, 발행 버튼
- **미결정**: 관리자 인증을 별도 role로 분리할지(Supabase RLS에서 admin role), 아니면 그냥 본인 계정만 하드코딩할지 — 1인 운영이라 후자로 단순화해도 무방
- **콘텐츠 캘린더 연동**: §2 발행 캘린더(주 1회 등)와 이 관리자 플로우가 실제로 맞물리는지 확인 필요

---

## 3. 다이어그램에 없는 추가 분기

### 3-1. 무료 미리보기 소진 후 재방문(비구독)
비로그인 유저가 무료 케이스 2개를 이미 다 봤다가 나중에 다시 방문한 경우 — 같은 케이스를 또 보여줄지, 가입 페이지로 바로 보낼지 결정 필요

### 3-2. 구독 중 축 3(믹스게임) 콘텐츠가 아직 없는 초기 상태
V1은 축 1(TDA)만 있고 축 2·축 3은 이후 개발이라, 초기 구독자가 대시보드에서 축 2·축 3을 눌렀을 때 "준비 중" 안내가 필요 — 빈 화면 대신 명확한 로드맵 안내로 기대치 관리

### 3-3. 랭킹/스트릭 리셋 시점
스트릭이 "이번 주" 기준인지 "연속 며칠" 기준인지 불명확 — PRD의 P0 요구사항에 "연속 정답 스트릭"만 있고 리셋 주기가 미정. 구현 전에 확정 필요

---

## 4. 우선 확정이 필요한 미결정 사항 (요약)

| 항목 | 결정 |
|---|---|
| 스트릭 리셋 주기 | **주 단위** — 이번 주 최소 1회 플레이 기준, 한 주라도 플레이 없으면 리셋. PRD의 "주간 재방문율 40%" 목표와 일치시킴 |
| 결제 실패 알림 채널·횟수 | **이메일만, 총 2회** — 실패 즉시 1회 + 유예기간 마지막날(3일차) 1회 |
| 무료 미리보기 재방문 정책 | **고정 케이스 유지** — 매주 교체하지 않음, 재플레이는 허용하되 매번 가입 CTA 노출 |
| 온보딩에서 관심 축 질문 여부 | 미정 (P1로 보류, V1은 축1만 있어 질문 의미 약함) |

---

## 5. 관리자(콘텐츠 발행) 플로우

지금까지 없던, Sno님이 매주 가장 자주 쓰게 될 화면입니다.

```
케이스 목록(draft/published 필터)
   ├─ 직접 작성 → 원본 케이스 폼(축·난이도·상황·정답·TDA근거·해설 입력)
   │                    ↓
   └─ AI 변형 생성 ← 원본 케이스 선택 → Sonnet 5 호출 → draft 3~5개 생성
                        ↓
                     검수 화면 (TDA 조항 재확인 체크리스트, status: draft 유지)
                        ↓ (검수 통과)
                     발행 (status: published, 발행 캘린더 슬롯에 배치)
                        ↓
                     구독자 노출 (캘린더 슬롯 도래 시 자동 공개)

     ※ 검수 반려 시 → 케이스 목록으로 복귀, draft 상태로 남아 재수정 가능
```

### 필요 화면 3개
1. **케이스 목록**: draft/published 상태별 필터, 축(1/2/3)별 필터, 발행 예정일 표시
2. **작성/편집 폼**: 축 선택, 난이도, 상황 텍스트, 선택지(또는 서술형 채점 기준), 정답, TDA 조항 번호, 해설 — 직접 작성과 AI 변형 결과 편집에 동일 폼 재사용
3. **검수 체크리스트**: "조항 번호가 원문과 일치하는가" "TD 재량 범위를 과도하게 단정하지 않았는가" 같은 항목을 체크해야 발행 버튼이 활성화되는 구조 권장 (실수로 미검수 콘텐츠가 발행되는 걸 방지)

### 설계 원칙
- **관리자 인증은 단순화**: 1인 운영이므로 별도 role 시스템 없이 본인 계정 하드코딩으로 충분 (RLS에서 admin role 분리는 팀이 커질 때 고려)
- **발행 캘린더와 연동**: §2 콘텐츠 커리큘럼의 발행 캘린더(축1 주 2회, 축2/3 보조)와 이 화면의 "발행 예정일" 필드가 같은 개념이어야 함 — 별도 스프레드시트로 캘린더를 관리하지 않고 이 화면 안에서 관리
- **AI 변형 생성은 선택적 기능**: V1 초기엔 직접 작성만으로도 충분 (파일럿 8개 케이스가 이미 이 방식), AI 변형은 콘텐츠 발행 속도가 병목이 될 때 추가해도 늦지 않음

