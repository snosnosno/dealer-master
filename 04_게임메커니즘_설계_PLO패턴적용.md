# 게임 메커니즘 설계 — PLO 연습 패턴 적용

**작성일:** 2026-08-20
**전제**: mixgame(PLO_Practice) 레포에서 검증한 두 게임 패턴을 TDA 판단 시뮬레이터에 적용. 코드는 재사용하지 않고 (Flutter → React 재구현), 게임 디자인 패턴만 가져온다.

---

## 1. 원본 패턴 분석

| 원본 게임 | 핵심 메커니즘 | 판단 유형 |
|---|---|---|
| 승자 맞추기 | 정적 정보(핸드) 제시 → 제한시간 내 단일 선택 | 스냅샷 판단 |
| 팟 계산 게임 | 정보가 시간에 따라 순차적으로 쌓임(액션 스트림) → 특정 타이밍에 계산/선언 | 실시간 추적 판단 |

이 두 유형이 TDA 판단 시뮬레이터에 그대로 대응됩니다. **스냅샷 판단**은 이미 PRD/AI채점 설계로 다뤘고, **실시간 추적 판단**은 아직 설계 안 된 새로운 모드입니다. 이 문서는 후자를 중심으로 설계합니다.

---

## 2. 신규 모드: "타임라인 판단" (Mode B)

### 컨셉
실제 TD 현장은 상황이 한 번에 다 주어지지 않습니다. 딜러 미스, 플레이어 항의, 클락 위반 같은 건 시간에 따라 사건이 전개되며, TD는 "언제 개입해야 하는지"를 실시간으로 판단해야 합니다. 팟 계산 게임의 "액션이 순차적으로 표시되다가 POT! 타이밍에 계산"하는 구조를 "사건이 순차적으로 표시되다가 개입 타이밍에 판정" 구조로 치환합니다.

### 플레이 플로우

```
1. 상황 시작 — 테이블 상태 초기값 표시 (예: 팟 크기, 플레이어 수, 스트리트)
2. 이벤트가 순차적으로 스트리밍 (0.8~1.5초 간격)
   예: "플레이어 A 올인 선언" → "카드 뒤집힘" → "딜러가 잘못된 카드 오픈" → "플레이어 B 항의"
3. 플레이어는 매 이벤트마다 "개입" 버튼을 누를 수 있음 (또는 끝까지 관망 가능)
4. "개입" 시점에 따라:
   - 너무 이르면: 아직 판단할 근거 부족 → 감점
   - 정확한 타이밍: 만점
   - 너무 늦으면(이벤트 스트림 종료): 늦은 개입 → 부분 점수
5. 개입 시점에 "어떤 조치를 할지" 서술형 입력 (기존 AI 채점 파이프라인 재사용)
6. 결과: 타이밍 정확도 + 판단 정확도 종합 점수
```

### 왜 이게 스냅샷 모드보다 나은 훈련 효과를 주는가
실전에서 TD가 실수하는 지점은 "규정을 몰라서"보다 "언제 끼어들어야 할지 놓쳐서"인 경우가 많습니다. 이 모드는 지식 테스트가 아니라 **타이밍 감각**을 훈련시켜서, 축 1(TDA)의 케이스 스터디보다 한 단계 더 실무에 가까운 콘텐츠가 됩니다.

---

## 3. 두 모드의 역할 분리

| 모드 | 대상 | 목적 | 개발 우선순위 |
|---|---|---|---|
| A. 스냅샷 판단 (기존 PRD) | 전 그룹 | 규정 이해도, 판단 정확도 | V1 (먼저 출시) |
| B. 타임라인 판단 (신규) | 현직 딜러/TD 중심 | 실전 타이밍 감각 | V1.1 (검증 후 확장) |

Mode A로 먼저 검증하고, 구독자 반응이 좋으면 Mode B를 "고급 콘텐츠"로 얹어서 상위 티어 근거로 쓸 수도 있습니다(향후 가격 티어 분리 옵션과 자연스럽게 연결).

---

## 4. 기술 설계

### 컴포넌트 구조 (React)
```
<TimelineSimulator>
  ├─ <TableStateDisplay />        // 초기 상태 (팟, 스트리트 등)
  ├─ <EventStream />              // 이벤트 순차 렌더링 (setInterval 또는 requestAnimationFrame 기반)
  ├─ <InterveneButton />          // 개입 버튼, 클릭 시 타이머 정지 + 타임스탬프 기록
  ├─ <JudgmentInput />            // 서술형 답변 입력 (기존 AI 채점 API 재사용)
  └─ <ResultPanel />              // 타이밍 점수 + 판단 점수 + TDA 해설
```

### 상태 관리
- 이벤트 스트림은 클라이언트에서 타이머로 진행 (서버 왕복 불필요 — 케이스 데이터를 한 번에 fetch 후 클라이언트에서 순차 표시)
- 개입 타임스탬프(이벤트 인덱스 기준 몇 번째에 눌렀는지)만 서버에 전송해서 채점

### DB 스키마 추가 (기존 `attempts` 테이블 확장)
```sql
-- attempts 테이블에 컬럼 추가
mode: 'snapshot' | 'timeline'
intervene_at_event_index: int (nullable, timeline 모드에서만 사용)
optimal_event_index: int (해당 케이스의 정답 개입 시점, cases 테이블에 저장)
timing_score: int (0~100)
```

### cases 테이블 확장 (timeline 모드용 콘텐츠)
```sql
events: jsonb  -- [{ index: 0, text: "...", delay_ms: 1000 }, ...]
optimal_intervene_index: int
```

### 채점 로직 (간단한 규칙 기반, AI 불필요)
```
timing_score = 100 - abs(사용자 개입 인덱스 - 정답 인덱스) * 15  (하한 0)
최종 점수 = timing_score * 0.4 + judgment_score(AI 채점) * 0.6
```
타이밍은 규칙 기반으로 즉시 계산(비용 없음), 판단 내용만 AI 채점 — 비용 효율성 유지.

---

## 5. 콘텐츠 제작 난이도

Mode A(스냅샷) 케이스보다 Mode B(타임라인) 케이스가 제작 부담이 더 큽니다 — 이벤트 시퀀스를 설계해야 하고, "정답 개입 시점"이 명확해야 채점이 공정합니다. 그래서:
- Mode B는 주 1회가 아니라 월 1~2회 정도로 발행 페이스를 낮게 잡는 걸 권장
- 원본 스토리(사건 시퀀스)를 Sno가 직접 설계하고, 이벤트 텍스트 배리에이션(문구만 다양화)은 AI 생성+검수 파이프라인(기존 설계) 재사용 가능

---

## 6. Action Items

1. [ ] Mode A(기존 PRD) 먼저 출시, 사용자 반응/리텐션 데이터 확보
2. [ ] Mode B 파일럿 케이스 3~5개 직접 설계 (이벤트 시퀀스 + 정답 개입 시점)
3. [ ] EventStream 컴포넌트 프로토타입 (타이머 기반 순차 렌더링)
4. [ ] cases/attempts 스키마 마이그레이션
5. [ ] 반응 좋으면 Mode B를 상위 티어(예: 15,000원대) 콘텐츠로 분리할지 검토
