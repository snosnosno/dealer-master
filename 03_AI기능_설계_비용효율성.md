> ⚠️ **이 문서는 2026-08-24 기준이라 현재 방향과 어긋난다.**
> **정본은 [`PRD.md`](PRD.md) 다.** 수익화 시점·서술형 AI 채점·로드맵이 모두 바뀌었다.
> 이 문서에서 아직 살아 있는 부분은 `PRD.md` §8 에 표로 정리돼 있다.

---

# AI 기능 설계 — 서술형 채점 & 콘텐츠 생성 파이프라인

**작성일:** 2026-08-20
**전제:** 퀴즈형 MVP를 "서술형 답변 + AI 채점"으로 업그레이드. 동일 AI 파이프라인을 콘텐츠(케이스 변형) 생성에도 재사용.

---

## 1. 왜 이 설계인가

역할극형 대화 시뮬레이션(완전 자유 진행)은 개발비/리스크가 크지만, **AI를 채점 한 지점에만 넣는 건 리스크가 훨씬 작으면서 판단력 훈련 효과는 크게 올라갑니다.** 게임 구조(퀴즈 플로우, 스트릭, 랭킹)는 기존 PRD 그대로 두고, "선택지 고르기"만 "서술형 답변 쓰기"로 바꾸는 정도의 변경입니다.

동시에 같은 AI 호출 패턴을 콘텐츠 제작(케이스 변형 생성)에도 써서, 1인 운영의 진짜 병목인 "주 1회 발행 페이스"를 완화합니다.

---

## 2. 아키텍처 추가 (기존 ADR-001 확장)

```
[유저] → 서술형 답변 입력
      ↓
[Next.js API Route / Supabase Edge Function]
      ↓ (Claude API 호출)
[Claude Haiku 4.5]
  - System prompt: 해당 케이스의 TDA 조항 원문 + 정답 판단 기준 (고정, 캐싱 대상)
  - User input: 유저의 서술형 답변
  - Output: 정오답 판정 + TDA 근거 기반 피드백 (2~3문장)
      ↓
[attempts 테이블에 저장] — 답변 원문, AI 피드백, 판정 결과
      ↓
[유저에게 결과 표시]
```

**모델 선택: Claude Haiku 4.5** (입력 $1 / 출력 $5 per MTok, 2026-08 기준). 채점은 복잡한 추론이 아니라 "정해진 기준에 답변이 부합하는지" 판단이라 Haiku 수준으로 충분합니다. Sonnet/Opus는 콘텐츠 생성(축 3 믹스게임처럼 창의적 케이스 작성)처럼 더 정교한 작업에만 예비로 남겨둡니다.

---

## 3. 비용 계산 (채점 기능)

**가정**
- 월 활성 구독자 1명당 시뮬레이터 플레이: 주 4회 × 4주 = 16회/월 (PRD의 리텐션 목표 기준)
- 케이스당 프롬프트 구성: TDA 조항+판단기준(시스템 프롬프트, 캐싱) 약 800토큰 + 유저 답변 약 100토큰 = 입력 900토큰
- AI 피드백 출력: 약 150토큰

**캐싱 미적용 시 (매 호출 풀토큰 과금)**
- 입력: 16회 × 900토큰 = 14,400토큰 = 0.0144 MTok × $1 = $0.0144
- 출력: 16회 × 150토큰 = 2,400토큰 = 0.0024 MTok × $5 = $0.012
- **월 1인당 약 $0.026 (~35원)**

**프롬프트 캐싱 적용 시** (시스템 프롬프트인 TDA 조항 부분은 케이스별로 고정 — 캐시 히트 시 입력가 10%)
- 캐시 대상 800토큰 중 첫 호출만 풀가, 이후 캐시 읽기(0.1x)
- 월 1인당 약 $0.015~0.02 (~20~27원) 수준까지 절감 가능

**결론**: 구독료 1만원 대비 AI 채점 원가는 **1인당 월 20~35원 수준 — 매출 대비 0.2~0.35%.** 비용은 사실상 무시 가능한 수준입니다. 걱정해야 할 건 API 비용이 아니라 (a) 답변 품질 검수 체계, (b) 오답 판정 리스크 쪽입니다.

**참고**: 위 가격은 2026년 8월 기준이며 Anthropic이 가격을 조정할 수 있으니, 실제 구현 시 https://docs.claude.com/en/docs/about-claude/pricing 에서 최신 요율을 재확인하는 걸 권장합니다.

---

## 4. 콘텐츠 생성 파이프라인 (케이스 변형)

**목적**: 원본 케이스 1개(직접 작성, TDA 근거 확인 완료) → AI가 상황 디테일만 바꾼 변형 3~5개 생성 → 발행 전 필수 검수.

```
[원본 케이스 작성] (Sno 직접, TDA 조항 근거 명시)
      ↓
[Claude Sonnet 5 호출] — "동일 판정 논리를 유지하되 상황만 바꿔서 N개 변형 생성"
      ↓
[status: draft] — DB에 초안으로 저장, 미공개
      ↓
[Sno 검수] — 판정 근거가 원본과 동일한지, 오류 없는지 확인
      ↓
[status: published] — 검수 통과 시에만 공개
```

**핵심 원칙**: AI가 생성한 케이스가 검수 없이 바로 발행되는 경로는 만들지 않습니다. 유료 교육 상품에서 잘못된 룰 해석이 노출되면 신뢰도에 직접 타격이 있기 때문에, "생성은 AI, 발행 승인은 사람"이라는 경계를 하드코딩 수준으로 지킵니다 (DB status 필드로 강제, UI에서 draft는 관리자만 조회 가능).

**콘텐츠 생성 비용**: Sonnet 5 사용($2/$10, 2026-08 인트로 가격 기준) 기준으로도 원본 1개당 변형 5개 생성은 입출력 합쳐 1회 호출에 몇 백원 수준 — 주 1회 케이스 발행에 필요한 변형 생성 비용은 월 단위로도 미미합니다.

---

## 5. 리스크 및 가드레일

| 리스크 | 대응 |
|---|---|
| AI가 TDA 조항을 잘못 인용 | 시스템 프롬프트에 조항 원문을 직접 포함(모델이 추론으로 만들어내지 않게), 케이스 발행 전 사람 검수 필수 |
| 서술형 답변 채점의 일관성 | 채점 기준을 프롬프트에 명시적 체크리스트 형태로 제공 (모호한 "잘 판단했는지" 대신 "다음 요소가 답변에 포함됐는지" 형태) |
| 유저가 악의적 프롬프트 인젝션 시도 | 유저 입력은 항상 별도 필드로 격리, 시스템 프롬프트 지시를 유저 입력이 덮어쓰지 못하도록 구조화 |
| API 장애 시 게임 진행 불가 | 채점 실패 시 "잠시 후 다시 시도" 안내 + 재시도 로직, 크리티컬 경로가 아니므로 폴백으로 정답 텍스트만 노출하는 옵션도 고려 가능 |

---

## 6. PRD 반영 사항 (기존 P0 요구사항 수정)

기존 PRD의 "케이스 판단 플레이 플로우"(P0-1)를 다음과 같이 수정 제안:
- 기존: 선택지(2~4개) 제시 → 선택 → 정답/오답 즉시 표시
- 변경: 서술형 답변 입력(짧은 텍스트, 100자 내외) → AI 채점 → 판정+피드백 표시 (응답 시간 고려해 로딩 UI 필요, Haiku 응답은 통상 1~2초 내외)
- 수용 기준 추가: Given AI 채점 API 실패, When 응답 지연/에러, Then 유저에게 재시도 안내 표시 (빈 화면 방치 금지)

## 7. Action Items

1. [ ] Claude API 키 발급 및 Haiku 4.5 모델 테스트 (claude-haiku-4-5-20251001)
2. [ ] TDA 조항 레퍼런스를 구조화된 텍스트로 정리 (시스템 프롬프트 캐싱 대상)
3. [ ] 채점 프롬프트 초안 작성 + 샘플 케이스 10개로 정확도 테스트
4. [ ] 케이스 변형 생성 프롬프트 설계 (Sonnet 5)
5. [ ] draft/published 상태 관리 스키마 반영
