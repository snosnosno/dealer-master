# 딜러마스터 — 브랜드/디자인 시스템

**작성일:** 2026-08-20
**참고**: UNIQN(유니콘, 블랙/골드 Midnight Craft)과 명확히 분리된 별도 아이덴티티

---

## 1. 브랜드 톤

UNIQN이 세련된 B2B 매칭 플랫폼의 무게감 있는 톤이라면, 딜러마스터는 **매일 훈련하는 게임화 서비스**답게 좀 더 가볍고 활기찬 톤으로 갑니다. 목표는 "권위 있는 판정 훈련"과 "매일 접속하고 싶은 습관형 서비스" 사이의 균형입니다.

## 2. 컬러 팔레트

| 역할 | 컬러 | 사용처 |
|---|---|---|
| 메인 (틸/그린) | 포커 테이블 펠트를 연상시키는 딥 틸 | 로고, 주요 아이콘, 등급 표시, 강조 텍스트 |
| 포인트 (앰버) | 따뜻한 앰버 | 스트릭 배지, 주의 환기가 필요한 요소, 등급 승급 알림 |
| 뉴트럴 (그레이) | 무채색 | 본문 텍스트, 보조 정보, 준비중 콘텐츠 |
| 배경 | 화이트 기반 (라이트) / 다크모드 자동 대응 | 전체 배경, 카드 |

### 2-1. 실제 값 (2026-08-30 기입)

이 문서는 오랫동안 역할 이름만 적고 **hex 값을 하나도 담지 않았다.** 그래서 구현이
`dealermaster_mockup.html` 의 값을 손으로 베껴 컴포넌트에 14종이 흩어졌다. 값의
정본을 여기 적고, 코드 쪽 정본은 `app/src/app/globals.css` 의 `@theme` 블록이다
(`--color-dm-*`). 둘이 어긋나면 이 표가 맞다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| teal-50 | `#E1F5EE` | 배지·선택 상태 배경, 정답 패널 |
| teal-100 | `#9FE1CB` | 다크모드 강조 텍스트 |
| teal-400 | `#1D9E75` | **다크모드 강조 텍스트**, 1,000 칩 |
| teal-600 | `#0F6E56` | 라이트모드 강조 텍스트, 테두리, 카드 뒷면 그라디언트 |
| teal-800 | `#085041` | 펠트, 주요 버튼, 결과 헤더 |
| teal-900 | `#04342C` | 펠트 테두리, 카드 뒷면 그라디언트 끝 |
| amber-50 | `#FAEEDA` | 부분 정답 패널 배경 |
| amber-400 | `#EF9F27` | SB/BB 태그, 로그 최신 줄, 100,000 칩 |
| amber-600 | `#854F0B` | **좌석 스택 숫자** (흰 배경 위 6.73:1) |
| amber-800 | `#4A2F02` | 앰버 배지 위 텍스트 |
| red | `#B23A2E` | 오답, 올인, 하트/다이아 |
| red-50 | `#FBEBE8` | 오답 패널 배경 |

칩 단위 색(§5-1)은 회색 `#B4B2A9` · 블루 `#378ADD` · 틸 `#1D9E75` ·
블랙 `#2C2C2A` · 퍼플 `#7F77DD` · 앰버 `#EF9F27`.

**왜 틸/그린인가**: UNIQN의 블랙/골드(고급스러움·B2B)와 대비되게, 포커 테이블 자체를 연상시키면서도 카지노 느낌의 자극적인 레드/골드보다는 차분하고 신뢰감 있는 톤을 선택. 매일 들어와서 학습하는 서비스이므로 눈이 편한 컬러가 중요.

## 3. UI 구성 원칙

- **등급/스트릭은 항상 상단에 노출** *(3단계에서 도입)*: 리텐션 장치이므로 대시보드
  진입 즉시 보이는 위치. **2단계 시점에는 등급도 스트릭도 대시보드도 없다** —
  점수 저장(`hand_sessions`)이 3단계 범위라 지금 만들면 더미 데이터 화면이 된다.
  2단계가 만든 것은 핸드 단위 결과 화면(축별 점수 + 판단 지점 리뷰)까지다
- **축(TDA/딜러교육/믹스게임)은 카드형 선택**: 축2·축3이 아직 없는 초기엔 "준비 중" 상태로 명확히 표시해 기대치 관리 (§08 유저플로우에서 확인된 엣지케이스)
- **CTA는 하나만**: "오늘의 케이스 풀기"처럼 화면당 핵심 행동 하나만 강조 버튼으로, 나머지는 보조 스타일
- **아이콘**: 법정·판정을 상징하는 아이콘(저울 등)을 축1에, 카드 아이콘을 축2·축3에 사용해 콘텐츠 성격을 직관적으로 구분

## 4. 적용 예시

대시보드 목업(위 대화에서 렌더링됨)에 적용된 구조:
- 상단: 서비스명 + 스트릭 배지(앰버, 불꽃 아이콘)
- 등급 카드: 틸 컬러로 강조된 현재 등급 + 원형 아이콘
- 축 선택: 2열 카드 그리드, 활성/준비중 상태를 색상 대비로 구분
- 하단: 단일 강조 CTA 버튼

전체 앱 목업(`dealermaster_mockup.html`)에서 8개 화면(랜딩/대시보드/TDA케이스/딜러교육/믹스게임/테이블뷰/결과/관리자)으로 확장 구현됨.

## 5. 공용 컴포넌트

### 5-1. 칩 단위 표준
축2·축3 모든 벳 계산 게임과 테이블뷰에서 공통으로 사용하는 칩 단위: **100 / 500 / 1,000 / 5,000 / 25,000 / 100,000**. 각 단위를 색상으로 구분(회색/블루/틸/블랙/퍼플/앰버)해 실제 카지노 칩셋과 유사한 시각 언어로 통일. 케이스 콘텐츠의 금액 예시도 가능하면 이 단위 조합으로 구성하는 걸 권장(예: 700이 아니라 600=500+100).

### 5-2. 홀덤테이블 좌석 배치 뷰 (table-felt)
6-max 오벌 테이블, 딜러버튼(D), SB/BB 태그, 좌석별 아바타+이름+스택, 중앙 팟 표시. 온라인 포커룸 UI에 가까운 톤(딥틸 펠트 배경, 화이트 아바타)으로 설계. 게임 B(딜링 시퀀스)의 버튼 이동 판단, 게임 A의 상황 시각화 등 여러 게임에서 공용 컴포넌트로 재사용 가능.

## 6. 다음 단계

- 로고 컨셉 (워드마크 vs 심볼) — 아직 미착수
- ~~실제 폰트 선택~~ **완료 (2026-08-30)** — Pretendard 로 확정. 라이선스는
  **SIL OFL 1.1**(웹 임베딩·상업적 사용 허용, 폰트 자체 판매만 금지)이라 프로덕션
  반영에 제약이 없다. 적용은 jsDelivr **동적 서브셋** CSS 를 `v1.3.9` 로 핀 고정.
  전체 Variable 은 2,009KB 지만 동적 서브셋은 유니코드 범위별 92개 청크 중 화면에
  실제로 쓰인 것만 받는다(CSS 13KB + 청크 몇 개). 폴백 스택에 OS 한글 서체를
  직접 적어 CDN 차단 환경에서도 기본값으로 떨어지지 않게 했다
- ~~다크모드 컬러 값 세부 조정~~ **완료 (2026-08-30)** — 실측으로 미달 3종을 찾아
  고쳤다. `#0F6E56` 강조 텍스트가 다크 배경에서 3.19:1, `#B23A2E` 가 3.34:1 로 AA
  미달이라 다크에서 각각 `#1D9E75`(5.85:1) · `#E8877A`(7.71:1) 로 뒤집게 했다
  (`--dm-accent` · `--dm-danger`).
  좌석 스택 숫자 `#EF9F27` 은 흰 배경 2.17:1 이었는데, 좌석 위치에 따라 배경이
  펠트가 되기도 페이지가 되기도 해서 **어떤 앰버도 양쪽을 통과하지 못했다.**
  색이 아니라 배경을 확정해 해결했다(흰 알약 + amber-600, 6.73:1)
- 좌석 아바타 — **넣지 않기로 결정 (2026-08-30)**. §5-2 가 "좌석별 아바타+이름+스택"
  을 적었지만, 그건 홀카드가 없는 좌석 배치 화면(목업 테이블뷰)의 구성이다.
  재생 화면의 좌석 상자는 폭 96px 안에 홀카드·이름·태그·스택·벳칩 5요소가 이미
  들어가 있고 세로가 펠트를 넘는다. 아바타는 이름·좌석번호가 주지 않는 정보를
  더하지 않으면서 세로만 늘린다. 축1 케이스 화면에서 다시 검토한다
