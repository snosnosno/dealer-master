# 다음 세션 프롬프트 — 2단계 설계와 계획이 끝났다. 이제 실행이다

- **날짜**: 2026-09-07
- **상태**: 1단계 **재미 게이트 통과** · 2단계 **설계·계획 완료, 코드 0줄**
- **브랜치**: `main` 하나 (`425edb3`)
- **다음**: `docs/superpowers/plans/2026-09-07-potaward-potlimit.md` 를
  **서브에이전트 방식(태스크당 새 에이전트)** 으로 실행

> 이 파일은 매번 **새로 쓴다.** 지난 세션의 지시 위에 배너를 덧붙이면 세 겹째부터
> 무엇이 유효한지 아무도 모르게 된다 — 이 레포가 실제로 그렇게 됐었다. 이전 내용은 git 에 있다.

아래 블록을 새 세션에 그대로 붙여넣으세요.

---

```
`docs/superpowers/plans/2026-09-07-potaward-potlimit.md` 를 실행한다.
설계는 `docs/superpowers/specs/2026-09-07-potaward-potlimit-design.md` 다. 둘 다 읽어라.

## 실행 방식 — 서브에이전트 (사용자 확인 2026-09-07)

`superpowers:subagent-driven-development` 스킬을 써라. **태스크 하나당 새 서브에이전트**를
띄우고, 태스크가 끝날 때마다 네가 직접 검토한다.

- 서브에이전트에게는 **그 태스크 블록만** 준다. 계획서가 실제로 자족적인지가 이 방식으로
  함께 검증된다 — 백지 문맥이 막히면 그건 계획서의 결함이다
- 서브에이전트의 "성공" 보고를 그대로 믿지 마라. **`git diff` 와 테스트 실행으로 독립
  검증한 뒤** 다음 태스크로 넘어간다
- 태스크는 각자 커밋으로 끝난다. `main` 이 아니라 **feature 브랜치**에서 작업하라

## 계획서의 모양

10개 태스크가 두 단계로 갈린다. **서로 독립이라 한쪽이 막혀도 다른 쪽은 간다.**

- **Phase A (태스크 1~6) — 팟 분배**
  1. `HandEvaluator` 포트 + `evaluatorFor(spec)` 어댑터
  2. 승자 판독을 그 원자로 이관 (동작 불변 — 기존 테스트 통과가 곧 검증)
  3. `awardPots` 하이로우 확장
  4~6. 생성기 · 화면 · 브라우저 검증
- **Phase B (태스크 7~10) — 팟리밋 계산**
  7. `BettingContext.pot` + `pl` 룰셋
  8~10. 생성기 · 숫자 입력 화면 · 브라우저 검증

**순서대로 간다.** 1→2→3 은 의존이 있고, Phase B 는 A 와 무관하다.

## 착수 전 기준선 (이 숫자가 안 나오면 뭔가 잘못된 것이다)

    cd "C:\Users\user\Desktop\dealer master\app"
    npx vitest run        # 450 통과 / 0 실패
    npx tsc --noEmit      # 0 errors
    npx playwright test   # 10 통과
    npm run lint          # 1 error — 아래 「알려진 것」 참조. 늘어나면 안 된다

## 태스크 7 에서 반드시 각오할 것

`BettingContext` 에 `pot` 을 **필수 필드**로 넣는다. 그러면 `BettingContext` 를 만드는
**모든 호출부가 한꺼번에 컴파일 에러**가 된다 (액션 판정 러시 쪽이 대부분이다).
의도된 것이고 `npx tsc --noEmit` 이 목록을 준다. **노리밋 경로에는 `pot: 0` 을 넣어라** —
`nlh` 는 이 값을 읽지 않으므로 동작이 바뀌지 않는다. 옵셔널로 만들어 피하지 마라.

## 두 드릴이 끝나면 — 재미 게이트

계획서 맨 끝에 두 드릴의 게이트 질문이 있다. **사람이 직접 한 판씩 하고 답해야 한다.**
통과 전에는 다음 하위 프로젝트로 넘어가지 않는다. 제한시간 값들은 전부 **초안**이고
게이트에서 확정한다 (승자 판독 40→30초와 같은 절차).

## 이미 끝난 일 — 다시 하지 마라

- 원자 셋(A-5 로우볼 · 오마하 2+3 · 오마하 하이), `GameSpec` 레지스트리, `Ruleset` 빚 청산
- **진행절차 드릴 전체** — 스텝·대본·판정·리듀서. 베팅 라운드에서 칩이 좌석 앞에 한 박자
  서고 로그가 좌석마다 액션을 남기는 것까지 (`ea89317`)
- **승자 판독 드릴 전체** — 하이·로우를 함께 묻고 동점(쿼터링)도 걸러내지 않는다.
  제한시간 30초 확정
- 화면 넷(홈 · 종목 허브 · 진행절차 · 승자 판독), Playwright 10개
- **2단계 설계와 계획** — 다시 설계하지 마라. 계획이 틀렸으면 고치되, 처음부터 다시 쓰지 마라

## 1단계 재미 게이트 결과 (2026-09-07, 사용자)

- 승자 판독: **제한시간 30초로 확정.** 나머지(지루함 · 로우 배분 · 하이/로우 두 줄 혼동)는
  「괜찮다」로 통과
- 진행절차: 결함 셋이 나왔고 **셋 다 고쳐서 머지됐다** — 베팅 시 칩이 안 보임 · 로그에
  누가 얼마 냈는지 없음 · 「블라인드 수거」를 「블라인드 포스팅」으로

## 알려진 것 — 이미 판정했으니 다시 올리지 마라

- **`npm run lint` 가 1 error 를 낸다**: `ProcedureScreen.tsx:41` `react-hooks/set-state-in-effect`.
  **하이드레이션 불일치를 고치려고 일부러 넣은 코드다**(커밋 a026e5a). 되돌리면 그 결함이
  되살아난다. 규칙을 끄는 게 아니라 다른 패턴을 찾아야 하고, 그건 별도 작업이다
- 진행절차 베팅 박자가 1100ms 인 것은 의도다. 520ms 로는 칩을 읽을 틈이 없다
- `hiWinners`/`loWinners` 구조가 닮았지만 비교 방향이 반대라 합치면 오히려 나빠진다
- 「동점 답이 실제로 출제된다」 테스트는 시드 30개(300문제) 통계에 기댄다
- `e2e` 의 `getByRole('button', {name:'1번'}).first()` 는 하이 줄이 먼저 렌더된다는 DOM 순서에 기댄다

## 이 레포에서 반드시 지킬 것

1. **정답은 엔진이 낸다.** 화면 코드에 규칙 판정을 쓰지 마라
2. **시드 결정론.** 생성기·화면 어디서도 렌더 중에 `Math.random()`/`randomSeed()` 를 쓰지 마라
3. **불변.** 리듀서와 순수 함수는 입력을 뮤테이트하지 않는다
4. **판정에 표시 라벨을 쓰지 마라.** `spec.betting`·`spec.eval` 코드값으로 갈라라
5. **자료에 없는 것을 짐작으로 채우지 마라.** 없으면 `throw` 로 정직하게 실패시키고,
   사용자에게 물어 출처를 `사용자 확인 (날짜)` 로 적어라
6. **실제 브라우저에서 눈으로 봐라.** 지난 두 세션 모두 코드 리뷰를 통과한 결함이
   브라우저에서 잡혔다 (React `key` 누락, `6번(6번) 콜` 문구). 매번 그렇다
7. **완료는 실행 출력으로만 주장한다**
8. **말투를 검사하라.** 이름 뒤 조사는 붙여 쓴다(`4번이다`), 명령형은 「…하세요」로 통일한다

## 하지 마라

- **범위를 넓히지 마라.** 딜링 룸 · 사고 처리 · 항의는 4단계, 라즈는 5단계다
- `lib/rush/` · `lib/action-rush/` 를 **격자로 옮기지 마라** — 450개 테스트가 걸려 있고
  얻는 게 없다. 노리밋 홀덤의 두 칸은 계속 `/rush` 를 가리킨다
- `RushScreen` 을 복사하지 마라. props 가 부족하면 props 를 늘려라
  (지금까지 성격이 다른 드릴 셋이 이 뼈대를 **한 줄도 안 고치고** 공유했다)
- `raiseCap` **값을 읽는 코드**를 쓰지 마라
- 칩 드래그를 되살리지 마라 — 사용자가 버렸다
- `potrush.best` · `actionrush.best` · `mixdeal.best` · `mixroom.best` ·
  `plo8.lowreading.best` · `plo8.winner.best` localStorage 키를 지우지 마라
- **PRD 를 다시 쓰지 마라** — 바꿀 이유가 생기면 사용자에게 먼저 물어라

## 사용자가 직접 지워야 하는 잔여물 (에이전트는 삭제 권한이 없다)

레포 루트에 추적되지 않는 파일이 있다. **커밋에는 안 들어갔고 지워도 안전하다.**

- `.claude/worktrees/grid-plo8/` (4.6MB) — 죽은 워크트리. 내용은 `.next` 빌드 산출물과
  `main` 에 이미 있는 문서뿐이다. `dealermaster_mockup.html` 의 차이는 줄바꿈 문자뿐이다
- `betting-beat.png` · `chips-on-felt.png` · `.playwright-mcp/` — 2026-09-07 세션이
  브라우저 확인하며 만든 것들
```
