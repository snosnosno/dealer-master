# 다음 세션 프롬프트 — 종목 다섯이 다 돈다. 이제 앱으로 옮길 차례다

- **날짜**: 2026-09-03
- **상태**: 축 3 프로토타입에 **종목 5 개**가 다 들어갔다. 연습이 메인, 딜링 룸은 시험이다.

> 이 파일은 매번 **새로 쓴다.** 지난 세션의 지시 위에 배너를 덧붙이면 세 겹째부터
> 무엇이 유효한지 아무도 모르게 된다 — 이 레포가 실제로 그렇게 됐었다. 이전 내용은 git 에 있다.

아래 블록을 새 세션에 그대로 붙여넣으세요.

---

```
제품 정체성: **"딜러가 되는 법"이 아니라 "판정을 내리는 법"**.
정본은 `PRD.md` 다. 루트의 번호 문서(00~13)는 낡았다 — 배너가 붙어 있다.
이번 단계의 목표는 사업이 아니라 **게임을 잘 만드는 것**이다. 계정·기록 서버 저장·결제·
AI 채점은 전부 뒤로 미뤄졌다(PRD §4). 다시 열지 마라.

## 지금 어디까지 왔나

축 3(믹스게임)은 프로토타입 하나에 두 모드가 들어 있다 —
`docs/superpowers/prototypes/mix-room.html` (2,412줄). 결정 기록은
`docs/superpowers/plans/2026-09-02-axis3-simulation-options.md` §0~§11.

**연습 모드가 메인이다** (2026-09-03, 재미 게이트에서 사용자가 방향을 바꿨다).
첫 화면이 종목 다섯을 고르는 목록이고, 딜링 룸은 그 아래 「시험」으로 내려갔다.
연습은 문제가 주어지지 않는다 — 딜러가 할 수 있는 행동 8 개가 **항상 같은 목록으로** 떠
있고 순서를 스스로 짠다. 틀리면 막고 무엇이 먼저인지 한 줄로 알려준다. 생명·시간 없음.

**딜링 룸**(시험) — 카드가 자동으로 날아가고(클릭하면 건너뜀), 손은 판단에서만 움직인다.
틀린 판단 3 번이면 끝나는 무한 딜링. 정답이어도 「다음」을 눌러야 넘어간다.

종목 5 개 — 7카드 스터드 · 2-7 트리플 드로우 · 라즈 · 스터드 하이로우 8 · **PLO 하이로우 8**.

### 이번 세션에 바뀐 것 두 가지

1. **「첫 액션」과 「벳 크기」가 `라운드 열기` 하나로 합쳐졌다.** 한 화면에서 두 단계다 —
   좌석을 지목하고 이어서 금액을 낸다. 둘 다 맞아야 정답이다. 지루하다는 피드백에서 왔고,
   두 판단이 원래 큐에서 붙어 나오던 것을 두 화면으로 쪼개 놨던 것이 원인이었다.
2. **PLO8 이 붙었다.** 커뮤니티 보드 · `family: 'flop'` 진행 · 홀 2 + 보드 3 강제 조합 ·
   팟리밋 계산(숫자 직접 입력). 앤티는 팟 계산에서 뺀다(가이드 p5).

## 이번 세션에 할 일 — 앱으로 옮긴다

프로토타입은 판정이 다 선다. **막힌 것은 앱 코드에 셋째 축 자리가 비어 있다는 것뿐이다.**

| 있는 것 | 위치 |
|---|---|
| 러시 공용 뼈대 | `app/src/lib/rush/session.ts` · `components/rush/RushScreen.tsx` |
| 축 1·2 | 그 뼈대를 공유한다 |
| 축 3 | **자리가 비어 있다** |

프로토타입이 단일 HTML 이라 그대로는 못 옮긴다. 옮기기 전에 무엇을 어떤 모양으로 자를지
정해야 한다 — `GameSpec`(데이터) · 룰셋(판정) · 화면의 경계는 `ADR-003` 에 이미 있다.

### 먼저 물어라

1. **연습과 딜링 룸을 둘 다 옮기나, 연습부터인가** — 연습이 메인이 됐으니 연습만 먼저
   옮기는 선택지가 있다. 딜링 룸은 프로토타입에 남겨 두고 나중에 붙여도 된다
2. **축 1·2 의 러시 뼈대를 재사용하나, 축 3 은 다른 모양인가** — 축 3 은 무한 딜링이고
   문제 목록이 아니다. 억지로 끼우면 양쪽이 다 나빠질 수 있다
3. **종목 5 개를 한 번에 옮기나** — 스펙은 데이터라 늘리는 건 싸지만, 판정 코드는 종목마다
   다르다. 스터드 하나로 먼저 세로 관통을 만드는 쪽이 안전할 수 있다

## 이미 있는 자산 — 다시 만들지 마라

| 자산 | 위치 | 상태 |
|---|---|---|
| 연습 + 딜링 룸 | `docs/superpowers/prototypes/mix-room.html` | 단일 HTML. 종목 5 · 사고 7 종 · 항의 6 종 |
| `GameSpec` (5 종목) | 같은 파일 `SPECS` | `eval: { hi, lo, qualifier }` · PLO8 은 `omaha: { hole: 2, board: 3 }` |
| 하이 평가기 | 같은 파일 `evalFive` · `bestFive` | 7장 중 최고 5장. `aceLow` 플래그로 2-7 도 낸다 |
| A-5 로우볼 + 8 자격 | 같은 파일 `evalLowA5` · `bestLowA5` | 라즈·스터드8 이 쓴다 |
| 오마하 강제 조합 | 같은 파일 `omahaCombos` · `bestOmahaHi` · `bestOmahaLow` | 홀 4 중 2 + 보드 5 중 3 |
| 팟리밋 | 같은 파일 `potBet(potNow, callAmt)` | `callAmt*2 + potNow`. 가이드 예제 두 개로 검증됨 |
| 스플릿 · 홀칩 | 같은 파일 `payoutLine` | 절반씩, 남는 칩은 하이에게 |
| 규칙 사실 | `docs/references/mixgame-facts.md` | 5 종목 전부 있다 |
| 자료 원본 | 로컬 `참고문서/` (gitignore) | 추출 명령은 `docs/references/README.md` 에 |
| 경계 결정 | `docs/superpowers/decisions/ADR-003` | 스펙=데이터 / 룰셋=판정 |

### 검증 스크립트를 다시 만들어 써라

프로토타입 **파일에서 코드 구간을 떼어** 그대로 돌리는 방식이다
(`html.indexOf('var CATKO =')` 처럼 잘라 `new Function` 으로 실행).
세션 스크래치패드에 세 개를 만들어 썼고, 이번 세션의 결과는 이렇다 —

- **문법 검사**: `<script>` 본문을 `new vm.Script` 에 넣어 본다
- **평가기 단위 테스트**: 하이·2-7·A-5·오마하 강제 조합·팟리밋 **48 개 통과**
- **브라우저 플레이**: `npx playwright` 로 연습 5 종목 완주 + 딜링 룸.
  MCP 는 이 머신에서 연결 실패하므로 스크립트를 직접 돌려라

평가기나 화면을 건드리면 세 개를 다시 만들어 돌려라.

## 자료 갭 — 그대로 남아 있다

**라즈의 브링인 방향과 이후 첫 액션은 자료에 없다.** 사용자가 확정해 줬다(2026-09-03):
3번째 카드는 **가장 높은** 업카드가 브링인(스터드와 반대), 4번째부터는 **보드 로우가 가장 좋은**
좌석이 먼저. 채점 화면 출처가 `사용자 확인 (2026-09-03)` 으로 뜬다 — 자료에서 온 판단과
눈으로 구별되게 두려는 것이다. 정본이 될 표
(`참고문서/KakaoTalk_..._02.png` 하단 "Stud (GAME)(BRING-IN)(First Action)")는 제목만 찍히고
잘렸고 다음 장은 다른 날짜 시트다 — 직접 확인했다.

**PLO8(플랍게임)의 사고 처리도 자료에 없다.** `mixgame-facts.md` §3 의 노출·스텁 표는
스터드와 드로우만 다룬다. 그래서 `mkIncident` 가 `family: 'flop'` 에서 `null` 을 돌려주고
**PLO8 은 사고 없이 판단만 낸다.** 짐작으로 채우지 마라 — 자료를 받으면 그때 붙인다.

## 이 레포에서 반드시 지킬 것

1. **정답은 엔진이 낸다.** 화면 코드에 규칙 판정을 쓰지 마라. 쓰고 싶어지면 엔진이 부족한 것이다
2. **자료에 없는 것을 짐작으로 채우지 마라.** 없으면 사용자에게 묻고, 답을 받으면
   출처를 `사용자 확인 (날짜)` 로 적어라
3. **실제 브라우저에서 눈으로 봐라.** 이번 세션에도 렌더로만 잡힌 결함이 있었다 —
   팟리밋 팟이 397,000 까지 부풀던 것은 스크린샷으로만 보였다
4. **완료는 실행 출력으로만 주장한다**
5. **말투를 검사하라.** 영어 직역·번역투가 계속 새어 나왔다. 지문을 전부 뽑아 읽어라

## 하지 마라

- PRD 를 다시 쓰지 마라 — 바꿀 이유가 생기면 사용자에게 먼저 물어라
- 딜링 룸의 형태를 다시 논의하지 마라. §1~§9 에서 정해졌다
- 연습과 딜링 룸의 순서를 되돌리지 마라 — 연습이 메인이다(§11)
- 「첫 액션」과 「벳 크기」를 다시 쪼개지 마라(§11)
- 칩 드래그를 되살리지 마라 — 사용자가 버렸다(§9)
- 계정·결제·AI 채점을 끌어오지 마라
- `potrush.best` · `actionrush.best` · `mixdeal.best` · `mixroom.best` localStorage 키를 지우지 마라
```
