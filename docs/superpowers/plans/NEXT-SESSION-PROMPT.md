# 다음 세션 프롬프트 — 엔진 코어 Task 8 리뷰부터 완주

아래 블록을 새 세션에 그대로 붙여넣으세요.

---

```
딜러마스터 시뮬레이터 엔진 코어 구현을 이어서 완주해줘. subagent 로 진행한다.

## 상황

Task 1~7 이 완료됐고 Task 8 은 구현·분할까지 끝나 리뷰만 남았다. Task 9·10 과
최종 whole-branch 리뷰가 남아 있다. 브랜치 review/simulator-engine-plan,
HEAD 는 7e512e8. 현재 npm test 155/155, npm run typecheck 0, npm run lint 0.

## 가장 먼저 할 일

`.superpowers/sdd/2026-08-25-simulator-engine-core/progress.md` 를 읽어라.
이게 복구 지점이다. 판정 R1~R25, 태스크별 상태, 유예된 minor, 다음 단계가
전부 거기 있다. 계획서를 처음부터 다시 읽지 마라 — 원장이 무엇이 끝났는지
말해준다.

그 다음 superpowers:subagent-driven-development 스킬을 로드하고,
원장이 가리키는 지점(Task 8 리뷰)부터 이어서 실행해라.

## 진행 방식

- 구현 subagent: model "opus"
- 리뷰 subagent: model "opus" — **fable 은 쓰지 마라.** 사용자가 명시적으로
  지시할 때만 fable 을 쓴다 (리뷰어가 fable 한도로 죽으면서 판정을 마치고도
  보고서를 안 남겨 전부 날아간 사고가 있었다)
- 태스크마다 커밋. 계획서에 커밋 메시지가 적혀 있다
- 태스크 간 의존이 있으므로 병렬 금지

## subagent 에게 반드시 넘길 것

- 해당 Task 브리프 파일 경로 (`.superpowers/sdd/.../task-N-brief.md`, 이미 생성돼 있다)
- 전역 제약과 절대 규칙 (원장 상단 + 아래)
- 그 태스크에 해당하는 판정 (원장의 Ruling 항목)

**리포트 파일을 먼저 완성한 뒤 최종 메시지를 보내라고 지시해라.** 에이전트가
결과를 마치고도 파일을 안 남기고 죽는 사고가 반복됐다. 워크스페이스는
git-ignored 라 커밋으로는 리포트를 확인할 수 없다.

## 절대 규칙 (subagent 프롬프트에 그대로 넣어라)

1. 테스트가 빨간불이면 테스트를 고치지 말고 구현을 고쳐라. 기대값은 TDA 규정에서
   나온 것이지 구현에서 나온 것이 아니다
2. 확률(roll 임계값)을 조정해 테스트를 통과시키지 마라. 계약이 확률적이면 계약을
   결정론으로 바꿔라. "생성 후 버리고 재시도"하는 리트라이 루프도 금지 —
   결정론이 흔들리고 종료가 보장되지 않는다
3. 구현 출력을 베껴 기대값으로 박는 골든값 테스트 금지
4. Math.random() 금지. 엔진 폴더에 react/next/@supabase import 금지
5. 파일 하나가 400줄을 넘기지 않는다 (테스트 파일도 포함). 넘칠 것 같으면 멈추고
   보고 — 혼자 쪼개지 마라
6. 계획서에 없는 것을 만들지 마라

## 세션 중 확립된 판정 중 남은 태스크에 계속 적용되는 것

- **R12-b**: 코드의 결함을 고치면 계획서·브리프의 해당 코드블록도 같이 고쳐라.
  계획서가 정본이라 코드만 고치면 결함이 문서에 보존됐다가 되살아난다
- **R17**: ruleRef 같은 유저 노출 문자열에 ⚠️ 를 넣지 마라. ⚠️ 는 "TDA PDF 원문과
  대조 못 했다"는 내부 표시이지 학습자에게 줄 정보가 아니다
- **R3**: 브리프의 Expected 테스트 개수는 추정치다. 실제값과 다르면 계획서 숫자를
  고쳐라 (테스트를 지워 맞추지 마라)
- **R8**: 브리프 요약 표기와 코드블록이 다르면 코드블록이 정본
- **불변식은 유도로, 가드는 throw 로**: 호출자의 실수가 조용히 상태를 오염시키게
  두지 마라. 다만 값에서 유도할 수 있으면 가드보다 유도가 낫다

## Task 8 리뷰에서 반드시 따지게 할 것

Task 8 은 직전 검토가 치명 결함을 발견한 두 곳 중 하나다 (다른 하나는 Task 5).
300 시드 실측으로 최소 레이즈 정답 오류 221/296, 선택지 겹침 296/296 이 나왔다.

리뷰 범위: BASE 107b46a → HEAD 7e512e8

- 최소 레이즈 정답이 nlh.minRaiseTo 와 일치하는가 (**1,000 이 정답, 1,200 이 아니다**)
- 선택지 겹침이 **구조적으로** 막히는가 (확률이 아니라)
- td_discretion 2경로(오픈 벳 아닌 언더콜 / 벳 앞 체크)가 단일 정답으로
  출제되지 않는가
- correctSeats 복수형이 공동 승자 핸드에서 실제로 여럿을 내는가
- 선택지 섞기가 시드 결정론적인가 (정답이 항상 같은 위치면 유저가 규칙 대신
  위치를 학습한다)

## Task 10 dispatch 에 반드시 실을 것

- **R21(4) 검증 갭**: 합법성 검증의 BettingContext 를 이벤트 로그에서 **규칙
  텍스트를 보고 독립 재구성**해라. generate.ts 의 헬퍼를 import 하면 안 된다.
  생성기와 테스트가 같은 규칙을 같은 코드로 재구성하면 둘이 같은 방향으로
  오해할 때 테스트가 통과한다
- **사이드팟은 require:['calculation'] 없이는 안 나온다.** 300 시드 실측으로
  require 없이 올인 0건·계산 판단 0건, 걸면 300/300. "일반 핸드에도 가끔
  섞이겠지"는 반증됐다
- **핸드마다 4축이 다 나온다고 가정하지 마라.** 액션 유효성 문제가 24%(71/300)
  핸드에서 안 나온다 — 오답 후보가 정답과 겹치면 출제하지 않는 의도된 동작이다
- **R7**: 완료 기준의 `grep -rn "Math.random" src/lib/simulator/` 는 문자 그대로는
  성립 불가다. 설명 주석이 함수명을 언급하기 때문. 실제 호출 0건으로 읽고,
  계획서 완료 기준 문구를 검사식
  `grep -rn "Math\.random(" src/lib/simulator/ | grep -vE ":\s*(\*|//)"` 로 정정해라
- **자격자 0명 팟**(R18)이 생기지 않는지 통합에서 확인 — return_uncalled 가
  옳게 나오면 도달 불가다. "잔여 팟 0" 검사가 이 전제를 지킨다
- 한 핸드 안에 같은 카드 2장 없음 / hole 이 좌석수 전체 길이 / 팟 금액이 100 배수

## 완료 기준

- npm test 전체 통과 / npm run typecheck 0 / npm run lint 0
- Task 10 의 "생성된 핸드가 규칙에 맞는가" 블록 통과 — 100 시드의 모든 플레이어
  액션이 nlh.validateAction 으로 합법 판정
- 핸드 100개에서 칩 보존, 음수 스택 없음, 잔여 팟 0
- Math.random 실제 호출 0건 (위 R7 검사식)
- 같은 시드가 같은 핸드·같은 판단 지점을 만듦

## 최종 보고에 반드시 포함할 것

- **발행 전 사람이 TDA 2024 PDF 원문으로 확인해야 하는 조항 번호 3개**:
  'TDA Rule 34 · 딜링 순서' / 'TDA Rule 21'(사이드팟) / 'TDA Rule 43-A'(레이즈 금액).
  학습자에게 조항 근거로 보이는 문자열이라 틀리면 잘못된 근거를 가르친다.
  레포에 PDF 가 없어 이 세션에서 확인 불가였다
- 원장의 Ruling 항목 전부 (R1~R25 + 새로 내린 것), 각각 틀렸을 때의 비용과 함께.
  사용자를 대신해 내린 결정이라 사용자가 읽고 되돌릴 수 있어야 한다
- 유예된 minor 목록 (최종 whole-branch 리뷰에서 트리아지)

## 보고

subagent 의 "성공" 보고를 그대로 믿지 마라. git diff 와 실제 테스트 실행으로
독립 검증한 뒤 보고해라. 통과하지 못한 것이 있으면 그대로 말해라.

**검수 방법 주의 (이 세션에서 두 번 틀렸다)**:
- 커밋이 없다고 "안 했다"고 단정하지 마라. 작업 트리와 리포트 파일까지 함께 봐라
- "지시가 반영됐나"를 볼 때는 바뀔 줄이 아니라 **새로 생길 것**을 찾아라.
  가드는 기존 줄을 바꾸지 않고 아래에 추가되는 형태다
```

---

## 참고 — 이 세션에서 끝난 것

**Task 1~7 완료, Task 8 구현·테스트 분할 완료(리뷰 대기).** 커밋 22개.

| Task | 산출물 | 줄 수 |
|---|---|---|
| 1 | `rng.ts` + vitest 환경 | 49 |
| 2 | `cards.ts` | 66 |
| 3 | `evaluate.ts` | 136 |
| 4 | `types.ts` / `reduce.ts` | 57 / 160 |
| 5 | `pots.ts` | 154 |
| 6 | `rulesets/types.ts` / `rulesets/nlh.ts` | 86 / 215 |
| 7 | `generate.ts` / `bots.ts` | 288 / 128 |
| 8 | `decisions.ts` | 257 |

테스트 155개, typecheck·lint exit 0.

### 리뷰가 잡아낸 실제 규칙 오류 (전부 plan-mandated 였다)

계획서가 시킨 대로 구현했는데 포커 규칙과 달랐던 것들이다. "브리프가 시켰다"는
감경 사유가 아니라는 원칙이 매번 옳았다.

| 판정 | 무엇이 틀렸나 |
|---|---|
| R19 | 벳 앞 체크를 `forced` 폴드로 확정 — 무효 액션이지 폴드 선언이 아니다 |
| Task 6 I1 | `kind:'allin'` 이 `canRaise` 검사를 우회 — "올인"이라 말하면 통과 |
| Task 6 I2 | `interpretChipPush` 가 오프닝 벳을 통째로 누락 — 밀어낸 칩이 증발 |
| Task 7 I1 | 쇼다운 공개 순서가 좌석 인덱스 순 — 버튼 위치와 무관 |
| Task 7 I2 | `require` 가 4종 중 3종을 조용히 무시 |

### 원 프롬프트가 남긴 미해결 3건의 처리

1. **타입 좁히기 미확인** → **종결.** `ValidationResult.ruling` 추가 후에도 판별
   유니온 좁히기가 정상 동작한다. 컴파일 실패를 기대하는 프로브로 확인 후 삭제
2. **Expected 테스트 개수가 추정치** → R3 로 매 태스크 정정 중
3. **TDA 조항 번호 3건 미검증** → ⚠️ 유지. R14 로 `Rule 21` 에도 ⚠️ 추가
   (근거 지위가 `Rule 20` 과 같은데 하나만 단정돼 있었다). **발행 전 사람 확인 필요**
