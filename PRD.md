# 딜러마스터 PRD

- **작성일**: 2026-08-31
- **개정**: 2026-09-04 — §3 · §6 · §7 · §10 · §11. "축 4개"를 **종목 × 드릴 격자**로 바꿨다 (사용자 확정)
- **상태**: 확정 (이 문서가 정본이다)
- **대체 대상**: `02_PRD_판단시뮬레이터.md` (2026-08-20) — 수익 모델을 정체성으로 삼았고, 서술형 AI 채점을 P0 로 뒀다. 둘 다 폐기한다.

> **이 문서와 다른 문서가 충돌하면 이 문서가 이긴다.**
> 루트의 번호 문서(`00`~`13`)는 2026-08-24 기준이라 현재 방향과 어긋나는 곳이 있다.
> 살아 있는 부분은 §8 에 명시했다.

---

## 1. 한 줄 정의

> **딜러마스터는 포커 판정력 훈련소다. 매일 3분 드릴로 손을 만들고, 주 1회 케이스로 판단을 만든다.**

이전 정의는 "월 9,900원 구독 멤버십"이었다. **수익 모델이 정체성 자리를 차지하고 있었다.**
결제를 뒤로 미룬 지금, 정체성은 "무엇을 잘하게 만드는가"여야 한다.

제품 정체성 한 줄은 [ADR-002](docs/superpowers/decisions/ADR-002-run-mode-dropped.md) 에서 이미 바뀌었다 —
**"딜러가 되는 법"이 아니라 "판정을 내리는 법".** 이 문서는 그 결정을 제품 전체로 확장한 것이다.

## 2. 풀려는 문제

TD·딜러·홀덤펍 운영자는 규정을 문서로는 알아도 **현장에서 즉시 판단하는 훈련 기회가 없다.**
룰북과 강의는 수동적 암기라 판단 속도로 이어지지 않는다. 국내에는 이 갭을 메우는 실무 훈련 도구가 사실상 없다.

훈련이 필요한 능력은 두 종류이고, 리듬이 다르다.

| 리듬 | 주기 | 훈련 대상 | 역할 |
|---|---|---|---|
| **빠름** | 매일 3분 | 계산·판독·규칙 적용 — 문제 자동 생성, 즉시 채점 | 다시 오게 만든다 |
| **느림** | 주 1회 10분 | 조항 적용과 재량 판단 — 저작 케이스 | 늘고 있다고 느끼게 만든다 |

**두 리듬이 공존하는 것이 이 제품의 형태다.** 격자(§6)는 빠른 리듬을 종목별로 펼친 것이고,
TDA 룰(§6-4)이 느린 리듬을 맡는다.

## 3. 목표

**한 종목의 격자 칸이 전부 차고, 다음 종목이 스펙 파일 하나로 붙는 것.** 그것뿐이다.

이번 단계는 사업이 아니라 **게임을 잘 만드는 것**이 목표다. 계정·기록·결제는 게임이 재미있다는 것이
확인된 뒤에 붙인다. 재미없는 게임에 계정을 붙이면 계정만 남는다.

### 완료의 정의 — 재미 게이트

드릴 하나가 완성됐다고 말하려면 **사람이 직접 한 세션을 끝까지 플레이하고** 세 가지에 답해야 한다.

1. **다시 하기가 눌리나?**
2. **지루한 유형이 있나?**
3. **제한시간이 맞나?**

셋 다 통과하지 못하면 다음 드릴로 넘어가지 않는다. 통과할 때까지 그 드릴을 고친다.
이 게이트는 2026-08-31 팟 판독 러시에서 실제로 작동한 방식 그대로다.

**재미 게이트와 격자의 판정은 다른 것이다.** 게이트는 사람이 답하고, 격자의 판정은
둘째 종목을 붙이는 데 코드가 얼마나 드는가로 답한다(§6). 둘 다 통과해야 한다.

### 측정 지표는 이번 단계에 없다

주간 재방문율·전환율 같은 지표는 **로그인이 붙은 뒤에** 의미가 생긴다.
지금 단계의 판정자는 지표가 아니라 게이트 3문항이다.

## 4. 비목표 (이번 단계에서 만들지 않는 것)

아래는 "안 한다"가 아니라 **"지금은 안 한다"**다. 순서를 지키기 위해 명시한다.

| 미루는 것 | 부활 시점 |
|---|---|
| 결제·구독·가격 | 한 종목의 격자가 다 차고 게이트를 통과한 뒤 |
| 로그인·계정 | 위와 같음. 그때 게스트/로그인 경계는 §7 안대로 |
| 서버 기록 저장 (`attempts` 등) | 로그인과 함께 |
| 서술형 답안 + AI 채점 | 로그인과 함께 (비용 상한이 로그인을 전제한다) |
| 일일 시드 챌린지 · 오답 리뷰 · 유형별 통계 | 기록 저장이 붙은 뒤 |
| 스트릭 · 등급 · 랭킹 | 기록 저장이 붙은 뒤 |
| 관리자 콘텐츠 발행 화면 | TDA 룰 케이스가 쌓이기 시작할 때 |
| 모바일 네이티브 앱 | 반응형 웹으로 충분 |

**이번 단계의 저장은 `localStorage` 하나다.** 최고 기록과 음소거 설정만 남긴다.

## 5. 사용자

TD 지망생 · 현직 딜러 · 홀덤펍 운영자. 셋의 공통 접점은 **규정을 현장에서 어떻게 적용하는가**다.
Sno 의 1,300명 업계 네트워크가 초기 사용자층이며, 콜드스타트 문제가 없다.

## 6. 격자 — 종목 × 드릴

이전 판본은 "축 4개"였다. 팟 판독은 노리밋 홀덤, 액션 판정도 노리밋 홀덤, 믹스게임은 축 하나에
종목 열 개가 뭉쳐 있었다. **훈련의 종류와 포커 종목이 한 이름 안에서 섞여 있었다.**

둘을 가른다. 훈련의 종류가 **드릴**이고 포커 종목이 **게임**이다. 제품은 그 둘의 격자다.

| | 진행절차 | 보드보기 | 팟리밋 | 팟 분배 | 로우 판독 | 사고 처리 | 액션 판정 |
|---|---|---|---|---|---|---|---|
| 노리밋 홀덤 | ⬜ | ✅ | — | ✅ | — | ⬜ | ✅ |
| PLO | ⬜ | ⬜ | ⬜ | ⬜ | — | ⬜ | ⬜ |
| **PLO8** | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| 7카드 스터드 | ⬜ | ⬜ | — | ⬜ | — | ⬜ | ⬜ |
| 스터드 하이로우 8 | ⬜ | ⬜ | — | ⬜ | ⬜ | ⬜ | ⬜ |
| 라즈 | ⬜ | ⬜ | — | ⬜ | ⬜ | ⬜ | ⬜ |
| 2-7 트리플드로 | ⬜ | ⬜ | — | ⬜ | ⬜ | ⬜ | ⬜ |
| 바두기 | ⬜ | ⬜ | — | ⬜ | ⬜ | ⬜ | ⬜ |

✅ 있음 · ⬜ 없음 · — 이 종목에 해당 없음

노리밋 홀덤의 ✅ 셋은 구 축 1·2 다. 팟 판독 러시가 보드보기(승자)와 팟 분배를,
액션 판정 러시가 액션 판정을 이미 채웠다. **격자는 그것들을 버리지 않고 자기 칸에 넣는다.**

**`—` 가 격자의 핵심이다.** 노리밋 홀덤에는 팟리밋 계산이 없고 하이 게임에는 로우 판독이 없다.
어느 칸이 존재하는지는 **`GameSpec`(§6-3)이 정한다.** 사람이 표를 손으로 관리하면 반드시 어긋난다.

### 완료의 정의가 바뀌었다

§3 의 재미 게이트 3문항은 그대로다. 다만 **무엇 하나가 게이트를 통과하는가**가 달라졌다.

> **한 종목의 칸이 전부 차고, 다음 종목이 스펙 파일 하나로 붙는 것.**

앞은 재미의 판정이고 뒤는 격자의 판정이다. 둘째 종목을 붙이는 데 코드를 많이 써야 한다면
격자가 틀린 것이다. 그때는 종목을 더 붙이기 전에 멈추고 격자를 고친다.

### 6-1. 드릴 7종

| 드릴 | 무엇을 묻나 | 상호작용 | 정답 출처 |
|---|---|---|---|
| **진행절차** | 지금 무엇을 할 차례인가 | **자기주도** | `GameSpec.streets` |
| **보드보기** | 이 판의 승자는 누구인가 | 퀴즈 | 하이 평가기 |
| **팟리밋** | 지금 최대 얼마까지 걸 수 있나 | 퀴즈 | 팟리밋 계산기 |
| **팟 분배** | 사이드팟·홀칩·스플릿을 어떻게 나누나 | 퀴즈 | `buildPots` · `awardPots` |
| **로우 판독** | 로우 자격자는 누구이고 어느 로우가 낮은가 | 퀴즈 | 로우볼 평가기 |
| **사고 처리** | 카드가 노출됐다·스텁이 부족하다 — 어떻게 수습하나 | 퀴즈 | 저작 케이스 |
| **액션 판정** | 이 액션이 합법인가 | 퀴즈 | `Ruleset` 판정 메서드 |

**상호작용은 두 형태뿐이다.** 진행절차만 자기주도다 — 문제가 주어지지 않고, 딜러가 할 수 있는
행동 전부가 팔레트에 늘 떠 있으며, 그중 무엇을 먼저 할지를 스스로 고른다. 나머지 여섯은 문제를 던진다.

**격자 뼈대는 이 둘을 다 견뎌야 한다.** 그래서 첫 하위 프로젝트가 이 둘을 함께 만든다(§10).

액션 판정은 노리밋 홀덤에 이미 완성돼 있다(구 축 2 — 액션 판정 러시). **다른 종목으로 넓히는 것은
우선순위가 가장 낮다** — 베팅 구조 원자가 먼저 서야 하기 때문이다.

### 6-2. 딜링 룸 — 드릴이 섞여 나오는 곳

드릴은 판단 하나를 떼어내 반복하는 연습장이다. 실제 딜링은 판단이 **연속해서** 나온다.

**딜링 룸은 한 핸드를 처음부터 끝까지 진행하는 모드다.** 절차를 밟다가 팟리밋을 계산하고,
사고가 터지면 수습하고, 쇼다운에서 하이와 로우를 갈라 팟을 지급한다. 드릴에서 따로 익힌 것이
흐름 안에서 섞여 나온다.

**연습장이 먼저다.** 딜링 룸은 드릴을 엮는 것이라 드릴이 서기 전에는 만들 수 없다.
격자가 검증되기 전에 손대지 않는다(§10 4단계).

### 6-3. `GameSpec` — 게임을 데이터로 정의한다

**격자가 성립하는 이유가 이 절이다.** 종목이 여덟이고 드릴이 일곱이면 칸이 쉰 개가 넘는다.
칸마다 트레이너를 따로 만들면 N 배 작업이 된다.
**게임을 데이터로 정의하고, 드릴은 그 데이터에서 생성한다.**

이것은 새 발상이 아니라 엔진이 이미 향하던 방향이다. `rulesets/types.ts` 의 주석이
그렇게 적혀 있다 — *"스터드·드로우·팟리밋을 나중에 데이터로 붙이기 위해 인터페이스 뒤에 둔다."*
`Ruleset` 에 `family` · `bettingStructure` · `holeCardCount` · `streets` 가 이미 있다.

#### 데이터가 되는 것 — `GameSpec`

```ts
type GameSpec = {
  id: 'nlh' | 'plo' | 'plo8' | 'stud7' | 'stud8' | 'razz' | '27td' | '27sd' | 'a5td' | 'badugi'
  name: { ko: string; en: string }
  family: 'flop' | 'stud' | 'draw'

  holeCardCount: number
  forcedBets: { kind: 'blinds' } | { kind: 'ante+bringin' }

  streets: {
    id: string
    deal: number                     // 이 스트릿에 몇 장
    faceUp: boolean                  // 업카드인가 다운카드인가
    burn: boolean                    // 번카드를 내리는가
    betting: boolean
    firstToAct:
      | 'left-of-bb' | 'left-of-button'
      | 'lowest-upcard'              // 스터드 3rd — 브링인
      | 'highest-upcard'             // 스터드 4th 이후
      | 'highest-upcard-by-suit'     // Stud Hi/Lo Regular — 스페이드 K 가 최고
      | 'lowest-hand'                // Stud Hi/Lo Regular 이후 라운드
    sizeMultiplier: 1 | 2            // 픽스리밋 5th 이후 2배
  }[]

  bettingStructure: 'no-limit' | 'pot-limit' | 'fixed-limit'
  raiseCap: number | null            // 픽스리밋 1벳 4레이즈 = 5

  draw?: { rounds: number; maxDiscard: number }

  eval: {
    hi: 'standard' | null
    lo: { aceIs: 'low' | 'high'; straightsFlushes: 'ignore' | 'count'; qualifier: null | 8 }
       | { kind: 'badugi' }
       | null
    mustUse?: { hole: 2; board: 3 }  // 오마하 강제 조합
  }
  split: { oddChipTo: 'hi' | 'high-hand' }
}
```

#### 코드로 남는 것 — 조합할 수 없는 원자

승자 판정은 데이터로 표현되지 않는다. 스펙은 **어느 평가기를 쓸지 고를 뿐**이고 평가기 자체는 코드다.

| 원자 | 상태 |
|---|---|
| `standard-high` | ✅ `evaluate.ts` 에 있음 |
| `lowball(aceIs, straightsFlushes, qualifier)` | ❌ 신규 |
| `badugi` | ❌ 신규 |
| 베팅 구조 3종의 min/max 계산 | 🟡 `no-limit` 만 있음 |
| 예외 상황 절차 (노출 카드·스텁 부족·오배열) | ❌ **스펙화되지 않는다.** 케이스로 저작한다 |

**로우 평가기는 2종이면 충분하다.** 2-7 과 A-5 는 별개 알고리즘이 아니라 같은 로우볼 평가기의
두 설정이다 — A-5 는 에이스가 최저이고 스트레이트·플러시를 무시하며(최고 5-4-3-2-A),
2-7 은 에이스가 최고이고 스트레이트·플러시를 인정한다(최고 7-5-4-3-2 무플러시).
**8-or-better 는 평가기가 아니라 자격 규칙**으로, A-5 설정에 `qualifier: 8` 을 얹은 것이다.
Badugi 만 진짜 별개다 — 4장이고 무늬 제약이 있다.

#### 평가기 2종이 여는 게임

| 게임 | family | hi | lo |
|---|---|---|---|
| NLH · PLO | flop | standard | — |
| **PLO8** (Omaha Hi/Lo) | flop | standard | a5 + qualifier 8 |
| 7-Card Stud | stud | standard | — |
| **Stud Hi/Lo 8** | stud | standard | a5 + qualifier 8 |
| **Stud Hi/Lo Regular** | stud | standard | a5, **자격 없음** |
| **Razz** | stud | — | a5, 자격 없음 |
| **2-7 Triple Draw · Single Draw** | draw | — | 2-7 |
| **A-5 Triple Draw** | draw | — | a5, 자격 없음 |
| **Badugi** | draw | — | badugi |

#### 이득 — 문제 생성기가 스펙 필드에 1:1 대응한다

| 스펙 필드 | 생성되는 문제 |
|---|---|
| `holeCardCount` | 이 게임의 홀카드는 몇 장인가 |
| `forcedBets` | 블라인드인가 브링인인가 / 브링인은 누구인가 |
| `streets[].firstToAct` | 이 스트릿의 첫 액션은 누구인가 |
| `streets[].burn` | 지금 번카드를 내리는가 |
| `bettingStructure` · `sizeMultiplier` · `raiseCap` | 지금 최대 벳은 / 이 스트릿의 캡은 |
| `draw` | 몇 장까지 교체 가능한가 / 스텁이 부족하면 |
| `eval.lo` | 로우 자격자는 누구인가 / 이 로우가 더 낮은가 |
| `split` | 팟을 어떻게 나누는가 / 홀칩은 누구에게 |

**게임을 추가하면 스펙 파일 하나로 문제 8종이 따라온다.** 평가기가 이미 있으면 코드는 0 줄이다.

게임 설명 화면도 같은 스펙에서 렌더한다. **규칙 설명과 문제의 정답이 한 원천에서 나오므로
서로 어긋날 수 없다.**

#### 세션 모드 — 개별 게임과 믹스

**게임 전환 규칙(레드·그린 버튼, 테이블당 핸드 수)은 `GameSpec` 에 넣지 않는다.**
그것은 게임의 규칙이 아니라 세션을 어떻게 구성하는가의 문제다. 스펙은 "이 게임이 무엇인가"만
말하고, "어떤 순서로 내놓는가"는 세션이 정한다.

```ts
type SessionSpec = {
  mode: 'single' | 'mix'
  games: GameSpecId[]        // single 이면 1 개, mix 면 여러 개
  questionCount: number      // 기본 10
}
```

- **개별 게임 모드** — 한 게임만 10문제. 그 게임을 익히는 용도
- **믹스 모드** — 문제마다 게임이 바뀐다. 화면 상단에 현재 게임을 크게 표시한다

**믹스 모드가 격자의 마지막 훈련이다.** 같은 보드라도 PLO8 이냐 Razz 냐에 따라 정답이 달라지고,
플레이어는 매 문제 "지금 무슨 게임인지"부터 다시 잡아야 한다. 실제 믹스게임 운영의 인지 부하가
그대로 재현된다.

전환 주기(매 문제인가 몇 문제 단위인가)는 프로토타입에서 정한다 — 매 문제 바뀌면 피로할 수 있다.

#### 함정 두 가지

1. **스펙을 처음부터 완전하게 만들지 않는다.** 문제로 내는 필드만 넣는다.
   쓰이지 않는 필드는 검증되지 않은 채 굳는다.
2. **게임 하나로는 추상화가 맞는지 알 수 없다.** 성격이 가장 다른 둘 —
   **7-Card Stud**(브링인·업카드·픽스리밋)와 **2-7 Triple Draw**(블라인드·드로우·로우볼) —
   를 같은 스펙으로 표현해보고, 그것이 되면 나머지를 붙인다.

**재미 가설**: 팟 배팅 금액과 로우 판독은 현직 딜러도 자주 틀린다.
계산 구조가 홀덤과 달라서, 러시를 다 푼 사람에게 새로운 벽이 된다.

### 6-4. TDA 룰 (느린 축)

상황 제시 → 선택지 판정 → **조항 근거와 입법 취지**.

**조항 근거는 WINNABLE 규정(한국어 56개 조)을 정본으로 쓴다.** TDA 영문보다 국내 사용자에게 실용적이고,
각 조항에 `[운영가이드] 입법 취지`가 붙어 있어 해설을 새로 쓸 필요가 없다.
WINNABLE 제2조가 "명시되지 않은 사안은 TDA 준용"이라고 규정하므로 TDA 와 충돌하지 않는다.

서술형 + AI 채점은 TDA 룰의 **다음 단계**다(§4). 서술형은 학습이지 게임이 아니고,
로그인·비용 상한을 전제한다. 먼저 선택지형으로 재미를 확인한다.

**재미 가설**: 격자가 속도라면 TDA 룰은 납득이다. "왜 그런가"를 읽는 맛.

## 7. 게임 공통 뼈대

격자의 모든 칸이 같은 뼈대를 공유한다. **문제 생성기와 화면 한 장만 드릴마다 새로 만든다.**

퀴즈형 드릴 여섯은 이 흐름이다.

```
문제 생성(시드 결정론) → 제한시간 → 응답 → 즉시 채점 → 근거 표시 → 다음 문제 → 세션 결과
```

자기주도인 진행절차는 제한시간도 없고 문제도 주어지지 않는다. 대신 이 흐름이다.

```
핸드 생성(시드 결정론) → 팔레트에서 선택 → 순서 판정 → 틀리면 무엇이 먼저인지 알려주고 다시 → 완주
```

- **정답은 반드시 엔진이 낸다.** 화면이 정답을 계산하면 정본이 둘이 된다 (러시에서 확립한 원칙)
- **시드로 재현 가능해야 한다.** 버그 재현·복기·공유가 전부 이것에 달려 있다
- **저장은 `localStorage`** — 최고 기록과 음소거뿐
- **화면층은 갈아 끼울 수 있어야 한다.** 지금은 DOM+CSS 로 그리고 나중에 캔버스로 갈 수 있다.
  그것을 가능하게 하는 것이 위의 첫 원칙이다 — 화면에 판정이 없으면 화면은 교체된다

### 게스트와 로그인의 경계 (로그인 도입 시 적용)

지금은 전부 무료지만, 로그인을 붙일 때 아래 경계를 쓴다. 결정해뒀으니 다시 논의하지 않는다.

| | 게스트 | 로그인 |
|---|---|---|
| 한 종목의 드릴 하나 | 고정 시드 **1세션 완주** | 무제한 |
| 나머지 드릴 | 고정 2문제 | 무제한 |
| 딜링 룸 | 한 핸드 | 무제한 |
| TDA 룰 | 선택지형만 | 서술형 + AI 채점 |
| 기록·스트릭·오답 리뷰 | 없음 | 있음 |

드릴 하나를 통째로 여는 이유: **한 세션 완주가 재미가 검증된 단위**다.
2문제에서 끊으면 게스트는 재미를 느끼기 전에 로그인 벽을 만난다.

## 8. 자료 정본

| 자료 | 용도 |
|---|---|
| WINNABLE 공식 대회 규정 v1.0 (한국어 56조) | 액션 판정·팟 분배의 조항 근거와 해설 |
| Mix Game Guide v1 (한국어) | 팟리밋·픽스리밋·스터드·드로우 규칙 — `GameSpec` 의 근거 |
| Mixed Game Rules & Protocols (2026-01) | 사고 처리 드릴 — 노출 카드, 스텁 부족, 밸런싱 |
| TD 시험 문제 11개 | 액션 판정 드릴의 문제 유형 명세이자 정답 검증용 |
| `05`·`11` 파일럿 케이스 30개 | TDA 룰(§6-4) 의 콘텐츠 재고 |

### 루트 번호 문서에서 살아 있는 것

| 문서 | 상태 |
|---|---|
| `01_아키텍처_설계.md` | 유효 (ADR-001 — Next.js/Supabase 선택 근거) |
| `05`·`11` 파일럿 케이스 | 유효 (콘텐츠 자산) |
| `06`·`07` 축2·축3 게임 설계 | 부분 유효 — 게임 아이디어는 살아 있고 순서는 이 문서가 이긴다 |
| `09_db_schema.sql` | 보류 — 기록 층은 §4 로 미뤄졌다. 되살릴 때 §9 의 결함을 먼저 고칠 것 |
| `12_design_system.md` | 유효 (틸 팔레트가 앱에 적용돼 있다) |
| `00`·`02`·`03`·`08`·`13` | **이 문서로 대체** — 구독·서술형 AI 채점·로드맵이 현재 방향과 어긋난다 |

## 9. 기록 층을 되살릴 때 먼저 고칠 것

`09_db_schema.sql` 과 `supabase/migrations/20260824000000_init_schema.sql` 은 현재 방향을 담지 못한다.
로그인을 붙이는 세션에서 아래를 먼저 처리한다.

| 결함 | 내용 |
|---|---|
| `attempts.case_id` NOT NULL | 러시·딜러·믹스 문제는 **생성**돼서 `cases` 행이 없다. 러시 기록을 저장할 수 없다 |
| `cases.track` 체크 제약 | `tda`/`dealer`/`mixgame` 뿐 — 러시 축이 빠져 있다 |
| `attempts.mode` 의 `timeline` | ADR-002 로 폐기된 C모드 잔재 |
| `streaks` 주 단위 | 러시는 "매일 3분" 리듬이다 |

**결정된 해법**: `attempts` 하나로 통합한다. `case_id` 를 nullable 로 풀고 생성 문제용 컬럼
(`drill_seed`, `drill_kind`, `question_index`)을 더한다. 러시는 시드만 저장하면 문제가 그대로 재현되므로
페이로드를 통째로 넣을 필요가 없다. 오답 리뷰·연속 기록·통합 통계를 축 구분 없이 한 쿼리로 뽑기 위함이다.

게스트 기록은 DB 에 넣지 않는다. `localStorage` 에 두고 첫 로그인 때 흡수한다.

## 10. 순서

격자를 **세로로** 판다 — PLO8 한 종목의 칸을 전부 채우고, 그다음 라즈가 스펙 파일 하나로
붙는지 본다. PLO8 을 첫 종목으로 고른 이유는 **드릴 7종이 전부 해당되는 유일한 종목**이기
때문이다. 팟리밋도 있고 로우도 있어서, 여기서 되면 나머지 종목은 부분집합이다.

```
1. 격자 뼈대 + PLO8 진행절차 + PLO8 로우 판독
   GameSpec 의 모양 · 자기주도와 퀴즈 두 상호작용 · 로우볼 평가기 · 홈 화면 재구성
2. PLO8 팟리밋 계산 + 팟 분배 (하이로우 스플릿으로 확장)
   베팅 구조 원자 · awardPots 확장
3. PLO8 보드보기 (오마하 2+3 강제 조합)
4. PLO8 딜링 룸 + 사고·항의
--- PLO8 재미 게이트 (§3) ---
5. 라즈를 스펙 파일 하나로 붙인다  ← 격자의 판정
   되면 스터드·드로우·PLO 를 잇는다. 안 되면 종목을 더 붙이기 전에 격자를 고친다
6. 연출 강화 (카드·칩 애니메이션), 필요하면 캔버스
--- 여기까지 게이트 통과 후 ---
7. 로그인 + 기록 층 (§9)
8. 서술형 + AI 채점, 일일 챌린지, 오답 리뷰
```

1 단계가 진행절차와 로우 판독을 **함께** 잡는 이유: 상호작용 형태가 정반대라(§6-1)
격자 뼈대가 둘 다 견디는지 한 번에 드러난다. 하나만 만들면 뼈대가 그 하나에 맞춰 굳는다.

**프로토타입 우선 원칙은 유지한다.** 새 드릴은 앱에 넣기 전에 단일 HTML 로 만들어 재미 게이트를
통과시킨다. 다만 진행절차·사고·항의는 `mix-room.html` 에, 팟리밋은 `mix-deal.html` 에 이미 서 있다 —
**있는 것을 다시 만들지 않는다.**

### 격자 밖에 남는 것

- **TDA 룰(§6-4)** — 조항 판정은 종목과 무관하다. 격자에 들어가지 않고 별도 축으로 남는다
- **액션 판정의 종목 확장** — 노리밋 홀덤 칸은 이미 차 있다(§6-1). 베팅 구조 원자가 선 뒤의 일이다

## 11. 열린 질문

- **진행절차 연습에 기록을 남길 것인가** — 지금은 막힘 횟수가 완주 화면에 떴다가 사라진다.
  다시 하게 만드는 힘이 거기서 나올 수도 있다. PLO8 진행절차를 **실제로 돌려 본 뒤에** 판단한다
- 퀴즈형 드릴의 제한시간 — 드릴마다 다른가, 종목마다 다른가
- 팟리밋 계산에 앤티를 포함할 것인가 (가이드: 대회사마다 다름)
- **사고 처리를 플랍게임에 낼 수 있는가** — `mkIncident` 가 `family: 'flop'` 에서 `null` 을 돌려준다.
  자료가 없다. 짐작으로 채우지 않는다 — §10 4단계 전에 사용자 확인이 필요하다
- 믹스 모드의 게임 전환 주기 — 매 문제인가, 2~3 문제 단위인가
- 믹스 모드 게임 조합의 기본값 — HORSE 처럼 고정할 것인가, 사용자가 고르게 할 것인가
