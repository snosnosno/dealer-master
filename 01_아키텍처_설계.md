# ADR-001: TD/딜러 멤버십 서비스 아키텍처

**Status:** Proposed
**Date:** 2026-08-20
**Deciders:** Sno (Snow Studio)

## Context

TDA 룰/딜러교육/믹스게임 콘텐츠를 월 1만원대 구독 멤버십으로 제공. 핵심 차별화 요소는 "판단 시뮬레이터"형 게임화 콘텐츠(제한시간 안에 케이스 판단 → 정답/해설 → 랭킹). 국내 PG(토스페이먼츠) 정기결제 연동 필요. UNIQN(React Native/Expo/Supabase)과는 별도 프로젝트로, 웹(React) 기반으로 결정됨 — 이유는 결제 전환율과 설치 장벽 때문.

제약 조건:
- 1인 개발, 유지보수 부담을 낮게 유지해야 함
- 초기엔 트래픽 적음(1,300명 네트워크 기반 검증 단계) — 과설계 금지
- 콘텐츠 제작이 병목(주 1회 발행 페이스) — CMS 편의성이 인프라 성능보다 중요
- 법적 리스크: UNIQN에서 겪은 직업안정법 이슈처럼, 콘텐츠가 "채용/알선"으로 해석되지 않도록 순수 교육 콘텐츠 경계 유지

## Decision

**Next.js(App Router) + Supabase(신규 프로젝트) + 토스페이먼츠 정기결제**로 구축한다. UNIQN의 Supabase 프로젝트와는 분리한다.

## Options Considered

### Option A: Next.js + Supabase (신규 프로젝트)

| Dimension | Assessment |
|-----------|------------|
| Complexity | 낮음 — Supabase가 Auth/DB/Storage 다 커버 |
| Cost | 낮음 (Vercel + Supabase 무료/저가 티어로 초기 충분) |
| Scalability | 초기 트래픽엔 충분, 나중에 필요시 확장 가능 |
| Team familiarity | 높음 (Supabase 경험 이미 보유) |

**Pros:** 빠른 개발, Auth 내장, RLS로 구독 상태별 콘텐츠 접근 제어 쉬움, UNIQN과 도메인/법적 분리로 리스크 격리
**Cons:** Supabase 프로젝트 2개 운영(각각 청구/모니터링 분리 관리 필요)

### Option B: Next.js + UNIQN 기존 Supabase 프로젝트 공유

| Dimension | Assessment |
|-----------|------------|
| Complexity | 중간 — 스키마 네임스페이스 분리 필요 |
| Cost | 낮음 (프로젝트 하나로 통합) |
| Scalability | 동일 |
| Team familiarity | 높음 |

**Pros:** 인프라 관리 포인트 하나로 절약
**Cons:** UNIQN(B2B 매칭 플랫폼, 사업자 등록 리스크 이력 있음)과 멤버십(B2C 콘텐츠 구독)이 같은 DB에 있으면 장애/보안 이슈가 서로 전이될 수 있음. 두 서비스는 목적도, 고객도, 법적 성격도 다름 — 분리가 안전

### Option C: 정적 사이트(Astro 등) + 별도 게임 위젯 임베드

| Dimension | Assessment |
|-----------|------------|
| Complexity | 낮음 (콘텐츠 발행만 본다면) |
| Cost | 매우 낮음 |
| Scalability | 충분 |
| Team familiarity | 중간 |

**Pros:** 콘텐츠 발행 자체는 가장 단순
**Cons:** 판단 시뮬레이터(상태 관리, 랭킹, 유저별 진행도)는 정적 사이트로 구현하기 어려움 — 결국 별도 앱 붙여야 해서 이중 구조가 됨. 처음부터 인터랙티브 앱 구조로 가는 게 낫다.

## Trade-off Analysis

핵심 트레이드오프는 **"UNIQN 인프라 재사용 vs 분리"**. 재사용 시 초기 비용은 줄지만, 두 서비스의 법적 성격이 다르다는 점(양면 매칭 플랫폼의 유료직업소개사업 리스크 vs 순수 콘텐츠 구독)이 결정적입니다. 콘텐츠 구독 서비스에 매칭/알선 뉘앙스가 조금이라도 섞이면 안 되므로, 데이터/도메인 분리가 곧 리스크 분리입니다. 신규 Supabase 프로젝트 비용은 무료 티어로 충분히 커버되는 수준이라 트레이드오프가 명확히 Option A 쪽으로 기웁니다.

## Consequences

- 새 Supabase 프로젝트/Vercel 프로젝트 셋업 필요 (반나절 작업)
- UNIQN 코드베이스와 완전 독립 — 배포/장애가 서로 영향 없음
- 인증(로그인)도 별도 — 이메일/소셜 로그인 UNIQN과 별개로 구현
- 추후 두 서비스 간 사용자 데이터 공유가 필요해지면(예: UNIQN 딜러에게 멤버십 크로스 프로모션) API 레벨에서 연동 설계 필요 — 지금은 범위 밖

---

## 시스템 구조

```
[사용자 브라우저]
      │
      ▼
[Next.js (Vercel)]
  ├─ 콘텐츠 페이지 (SSR/SSG) — 케이스 스터디, 커리큘럼 아티클
  ├─ 판단 시뮬레이터 (클라이언트 컴포넌트, 상태 관리)
  ├─ 인증 (Supabase Auth)
  └─ 결제 플로우 (토스페이먼츠 SDK)
      │
      ▼
[Supabase (신규 프로젝트)]
  ├─ Auth (이메일/소셜 로그인)
  ├─ Postgres
  │   ├─ users, subscriptions (구독 상태)
  │   ├─ cases (TDA 케이스 콘텐츠)
  │   ├─ curriculum_modules (딜러교육)
  │   ├─ mixgame_modules (믹스게임)
  │   ├─ attempts (유저별 시뮬레이터 시도 기록)
  │   └─ rankings (주간 랭킹 집계)
  ├─ RLS 정책 — 구독 상태(active/expired)에 따라 콘텐츠 접근 제어
  └─ Storage (이미지/향후 영상 자산)
      │
      ▼
[토스페이먼츠]
  ├─ 정기결제(빌링) API
  └─ Webhook → Supabase Edge Function → subscriptions 테이블 갱신
```

## 데이터 모델 핵심 (요약)

- `subscriptions`: user_id, status(active/paused/canceled), plan_tier, billing_key(토스), next_billing_date
- `cases`: id, track(tda/dealer/mixgame), difficulty, situation_text, options(json), correct_option, explanation, tda_ref
- `attempts`: user_id, case_id, selected_option, is_correct, response_time_ms, created_at
- `rankings`: 주 단위 집계 뷰(materialized view or scheduled function) — attempts 기반 스트릭/정답률 계산

## 콘텐츠 접근 제어

- 무료 미리보기: 케이스 1~2개 공개(랜딩페이지 유입용)
- 구독 유효(active): 전체 콘텐츠 + 시뮬레이터 + 랭킹 참여 가능
- 구독 만료: 콘텐츠 열람 차단, 과거 진행 기록(attempts)은 보존(재구독 시 이어보기)

## 결제 연동 흐름 (요약)

1. 유저가 구독 신청 → 토스페이먼츠 빌리키 발급(카드 등록)
2. 매월 자동 결제 API 호출(스케줄러 — Supabase Edge Function cron 또는 Vercel Cron)
3. 결제 성공/실패 Webhook 수신 → subscriptions 테이블 갱신
4. 결제 실패 시 유예 기간(예: 3일) 후 접근 차단 + 알림 이메일

## Action Items

1. [ ] Vercel + Next.js 프로젝트 초기화
2. [ ] Supabase 신규 프로젝트 생성 + 스키마 마이그레이션
3. [ ] 토스페이먼츠 가맹점 심사/정기결제(빌링) API 신청 (심사 기간 고려해서 최우선 착수)
4. [ ] 판단 시뮬레이터 컴포넌트 프로토타입
5. [ ] RLS 정책 설계 및 테스트
