# 딜러마스터 — Claude Code 핸드오프 문서

**작성일:** 2026-08-20
**목적**: 이 문서 하나로 개발 착수에 필요한 모든 결정사항과 스펙을 파악할 수 있도록 정리. 세부 근거는 각 원본 문서 참조.

---

## 1. 프로젝트 한 줄 요약

**딜러마스터** — 포커 TD(토너먼트 디렉터)/딜러 실무자를 위한 판단력 훈련 게임화 구독 서비스. TDA(Poker Tournament Directors Association) 규정 기반 케이스를 서술형으로 풀고 AI가 채점하는 방식. 월 9,900원 구독. Sno(Snow Studio)의 1,300명 포커 업계 네트워크 기반으로 검증 완료, 결제 전 개발 착수 단계.

**UNIQN과의 관계**: 완전히 별도 프로젝트. UNIQN(양면 매칭 B2B 플랫폼, RN/Expo/Supabase)과 인프라·법적 성격 모두 분리. 딜러마스터는 웹(Next.js) + 신규 Supabase 프로젝트.

---

## 2. 기술 스택 (확정)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | Next.js (App Router) | 결제 SDK 연동, SEO, 설치 장벽 없는 웹 접근성 |
| 배포 | Vercel | Next.js 표준 배포처 |
| 백엔드/DB | Supabase (신규 프로젝트, UNIQN과 분리) | Auth+Postgres+Storage+RLS 통합, 팀 경험 보유 |
| 결제 | 토스페이먼츠 정기결제(빌링) | 국내 표준 PG, 구독 결제 지원 |
| AI 채점 | Claude Haiku 4.5 (`claude-haiku-4-5-20251001`) | 저비용, 채점 작업에 충분한 성능 |
| AI 콘텐츠 생성(향후) | Claude Sonnet 5 | 케이스 변형 생성용, V1 이후 |

전체 아키텍처 트레이드오프 분석: `01_아키텍처_설계.md` (ADR-001)

---

## 3. DB 스키마

**실행 가능한 전체 SQL**: `09_db_schema.sql` — 그대로 Supabase SQL Editor에 붙여넣어 실행 가능.

핵심 테이블 요약:
- `subscriptions` — 구독 상태(pending/active/past_due/canceled), 토스 빌링키, 유예기간
- `cases` — 케이스 콘텐츠 (track: tda/dealer/mixgame, status: draft/published/archived)
- `attempts` — 유저별 시도 기록 (서술형 답변, AI 피드백, 정오답)
- `streaks` — **주 단위** 스트릭(연속 일수 아님, 확정사항), 등급(junior/senior/master)
- `weekly_rankings` — materialized view, cron으로 매일 refresh 필요

RLS 정책 포함: 구독 active 상태에서만 published 콘텐츠 접근, 관리자(created_by 본인)만 draft 조회/수정.

---

## 4. AI 채점 시스템

**전체 프롬프트**: `10_grading_prompt_draft.md`

핵심 요약:
- 모델: Haiku 4.5, 시스템 프롬프트에 케이스 정보(조항 원문 포함) 고정 + prompt caching 적용
- 유저 서술형 답변 → JSON 응답(`verdict: correct/incorrect/partial`, `feedback`) 파싱
- 유저 입력은 별도 필드로 격리해 프롬프트 인젝션 방지
- 비용: 1인당 월 20~35원 수준 (구독료 9,900원 대비 0.2~0.35%) — 비용 걱정 불필요, 정확도가 우선순위
- 개발 시 테스트 계획: 파일럿 케이스 8개 × 정답/오답/부분정답 샘플 3개 = 24개 테스트셋으로 정확도 검증 먼저 수행

---

## 5. 콘텐츠 (런칭 준비 완료)

**축1(TDA) 케이스 30개** 작성 및 원문 검수 완료 — `05_pilot_cases_v1.md`(1~8) + `11_pilot_cases_v2.md`(9~30)
- PRD 목표치(런칭 전 20~30개) 충족
- 각 케이스: 상황/선택지 또는 서술형 채점기준/정답/TDA 조항 근거/해설 포함
- **개발 시 필요 작업**: 이 30개를 `cases` 테이블에 seed 데이터로 입력하는 스크립트 작성 (아직 미작성 — TODO)

**축2(딜러교육)·축3(믹스게임) 게임 설계**: `06_axis2_axis3_simulation_games.md`, 예시는 `07_axis2_axis3_examples.md` — V1 이후 개발 대상, 지금은 설계만 존재

---

## 6. 핵심 유저플로우

전체 상세: `08_user_flow_review.md` (다이어그램은 대화 내 렌더링됨, 텍스트로 재기술)

### 6-1. 가입 전 유입
```
랜딩페이지 → 무료 미리보기 케이스 1~2개(고정, 매주 교체 안 함) → 3번째 시도 시 가입 유도
→ 회원가입(이메일/소셜) → 구독 결제(토스페이먼츠 9,900원) → 온보딩 → 대시보드
```

### 6-2. 가입 후 핵심 루프
```
대시보드(축1/2/3 선택, 등급/스트릭 표시) → 케이스 플레이(서술형 입력 → AI 채점 → 피드백)
→ 결과(스트릭·등급·랭킹 갱신) → 대시보드 복귀 (매주 반복)
```

### 6-3. 결제 실패 분기
```
결제 갱신 실패 → 이메일 알림(실패 즉시 1회 + 3일차 1회, 총 2회) → 유예 3일 → 접근 차단(과거 기록 보존) → 재구독 유도
```

### 6-4. 관리자(Sno) 콘텐츠 발행 플로우 — 별도 화면 필요
```
케이스 목록(draft/published 필터) → 직접 작성(폼 입력) 또는 AI 변형 생성(V1 이후)
→ 검수(TDA 조항 재확인 체크리스트 통과해야 발행 버튼 활성화) → 발행(status: published, 캘린더 슬롯 배치)
→ 구독자 노출
```
- 1인 운영이므로 관리자 인증은 role 시스템 없이 본인 계정 하드코딩으로 단순화
- 발행 캘린더: 축1은 주 2회, 축2/3(개발 후) 보조

---

## 7. 확정된 정책/숫자 (구현 시 그대로 적용)

| 항목 | 값 |
|---|---|
| 구독료 | 월 9,900원 |
| 무료 미리보기 | 고정 케이스 1~2개, 재방문 시 재플레이 허용 + 가입 CTA 노출 |
| 스트릭 리셋 | 주 단위 (이번 주 최소 1회 플레이, 한 주라도 없으면 리셋) |
| 결제 실패 유예 | 3일, 이메일 알림 2회(즉시+3일차) |
| 등급 체계 | junior → senior → master (승급 기준 수치는 미정, 콘텐츠 기획 시 확정 필요) |
| AI 채점 모델 | Haiku 4.5, 콘텐츠 생성은 Sonnet 5 |

---

## 8. 브랜드/디자인

전체: `12_design_system.md`, 전체 8화면 목업: `dealermaster_mockup.html`(랜딩/대시보드/TDA케이스/딜러교육/믹스게임/테이블뷰/결과/관리자)

- **메인 컬러**: 틸/그린 (포커 테이블 펠트 연상, UNIQN 블랙골드와 분리)
- **포인트 컬러**: 앰버 (스트릭 배지, 승급 알림 등 주목 요소)
- **UI 원칙**: 등급/스트릭 상단 고정 노출, 축 선택은 카드형(준비중 상태 명확히 구분), 화면당 CTA 버튼 하나만 강조
- **칩 단위 표준**: 100/500/1,000/5,000/25,000/100,000 (색상 구분), 축2·축3 전 게임과 테이블뷰에서 공통 사용
- **테이블뷰 컴포넌트**: 6-max 오벌 홀덤테이블(딜러버튼·SB/BB·좌석별 스택·중앙 팟) — 여러 게임에서 재사용 가능한 공용 시각 요소
- 로고 컨셉, 웹폰트 라이선스 확정은 미착수 (Phase 3 이후 작업)

---

## 9. 아직 결정되지 않은 사항 (개발 중 확정 필요)

| 항목 | 비고 |
|---|---|
| 등급 승급 기준 구체 수치 (몇 문제 맞아야 senior/master인지) | 콘텐츠 기획 필요 |
| 케이스 변형 생성 워크플로우 (draft→published 세부 UI) | V1 이후로 보류 |
| TDA 규정 use policy 확인 | **법적 리스크, 개발과 별개로 최우선 확인 필요** — PokerTDA.com use policy 미확인 상태 |
| 이용약관/환불정책 | 토스페이먼츠 심사에 필요, 아직 초안 없음 |
| 랜딩페이지 실제 카피/디자인 | 목업 없음 |

---

## 10. 개발 착수 순서 제안

1. TDA use policy 확인 (법적 리스크, 콘텐츠 공개 전 반드시)
2. Next.js + Vercel 프로젝트 초기화
3. Supabase 신규 프로젝트 생성 → `09_db_schema.sql` 그대로 실행
4. 토스페이먼츠 가맹점 심사 신청 (병렬 진행, 심사 기간 소요)
5. Auth(이메일/소셜 로그인) 구현
6. 케이스 30개 seed 스크립트 작성 + DB 입력
7. AI 채점 API 연동 (`10_grading_prompt_draft.md` 프롬프트로 Haiku 4.5 테스트, 24개 테스트셋으로 정확도 검증)
8. 무료 미리보기 + 케이스 플레이 UI (§6-2 플로우, §8 브랜드 적용)
9. 관리자 콘텐츠 발행 화면 (§6-4)
10. 결제 연동 (토스 심사 통과 후)
11. 베타 오픈

---

## 부록. 전체 문서 목록

| 파일 | 내용 |
|---|---|
| `00_마스터_기획문서.md` | 전체 기획 개요, 로드맵 |
| `TD딜러_멤버십_커리큘럼.md` | 콘텐츠 커리큘럼(축1/2/3) |
| `01_아키텍처_설계.md` | ADR, 시스템 구조 |
| `02_PRD_판단시뮬레이터.md` | 판단 시뮬레이터 PRD |
| `03_AI기능_설계_비용효율성.md` | AI 채점/생성 설계, 비용 계산 |
| `04_게임메커니즘_설계_PLO패턴적용.md` | 타임라인 판단 모드 설계 |
| `05_pilot_cases_v1.md` | 축1 케이스 1~8 |
| `06_axis2_axis3_simulation_games.md` | 축2·축3 게임 설계 (게임 A~E) |
| `07_axis2_axis3_examples.md` | 축2·축3 게임별 구체 예시 |
| `08_user_flow_review.md` | 전체 유저플로우 + 관리자 플로우 |
| `09_db_schema.sql` | 실행 가능한 DB 스키마 |
| `10_grading_prompt_draft.md` | AI 채점 프롬프트 |
| `11_pilot_cases_v2.md` | 축1 케이스 9~30 |
| `12_design_system.md` | 브랜드/디자인 시스템 |
| `dealermaster_mockup.html` | 전체 앱 목업 (8화면, 인터랙티브 HTML) |
| `13_claude_code_handoff.md` | 본 문서 |
